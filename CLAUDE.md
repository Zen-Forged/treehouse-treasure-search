## HOW TO START A NEW SESSION

1. Run `th` in Terminal — reads CLAUDE.md and copies to clipboard
2. Open claude.ai → New conversation
3. Paste the opener below, fill in CURRENT ISSUE from clipboard

```
PROJECT: Treehouse Finds — Zen-Forged/treehouse-treasure-search — app.kentuckytreehouse.com
STACK: Next.js 14 App Router · TypeScript · Tailwind · Framer Motion · Anthropic SDK · Supabase · SerpAPI · Vercel
Filesystem MCP is connected at /Users/davidbutler/Projects/treehouse-treasure-search
Read CLAUDE.md, CONTEXT.md, and docs/DECISION_GATE.md. Then run the session opening standup from MASTER_PROMPT.md.

CURRENT ISSUE:
[paste here]
```

## HOW TO END A SESSION

Run `/session-close` — replaces the old `thc` alias with the full close protocol (tombstoning, memory updates, commit + push, PR creation).

---

## DOCUMENT MAP

This file is the **live whiteboard** — only the current session's starting point. Everything else is elsewhere:

| Need | Read |
|---|---|
| Architecture, schema, routes, API table, lib + component catalog, auth pattern, DNS state, known gotchas, debugging commands | `CONTEXT.md` |
| Operating constitution: brand rules, tech rules, risk register, decision gate, agent roster | `docs/DECISION_GATE.md` |
| Session structure, HITL indicator standard, Docs agent + Design agent operating principles, blocker protocol | `MASTER_PROMPT.md` |
| Historical session close summaries (sessions 1–30, growing) | `docs/session-archive.md` |
| Canonical design system v1.1l + v1.2 build spec | `docs/design-system.md` + `docs/design-system-v1.2-build-spec.md` + `docs/mockups/` |
| Canonical vendor onboarding (3 flows) | `docs/onboarding-journey.md` |
| Supabase OTP email templates (HITL paste target) | `docs/supabase-otp-email-templates.md` |
| In-mall SQL triage recipes | `docs/admin-runbook.md` |
| Active bugs + deferred items + resolved history | `docs/known-issues.md` |
| Pre-beta QA walk runbook (T4d) | `docs/pre-beta-qa-walk.md` |
| Window share QA walk runbook (Q-007 session 41) | `docs/share-booth-qa-walk.md` |
| Queued sessions (scoped work sequenced behind something else) + operational backlog | `docs/queued-sessions.md` |
| Roadmap (Beta+) — epic-level captured items not yet scoped | `docs/roadmap-beta-plus.md` |
| Tech Rule candidates queue (firing counts + promotion status) | `docs/tech-rules-queue.md` |

---

## TERMINAL COMMAND FORMATTING CONVENTION
> When Claude surfaces multiple terminal commands to run in sequence, each goes in its own fenced block. This lets David copy one at a time and verify each before running the next.

**Do this:**

```bash
npm run build 2>&1 | tail -30
```

```bash
git add -A && git commit -m "..." && git push
```

**Not this** (chained separate commands in one block that invite blind paste):

```bash
npm run build 2>&1 | tail -30
git add -A && git commit -m "..." && git push
```

Exception: a single chained command with `&&` stays in one block — that's one atomic action by design.

---

## ✅ Session 118 (2026-05-06) — R17 Arc 1: geolocation primitives shipped in isolation — 6 runtime commits + clean iPhone QA + close

David opened with `/session-open`; standup recommended **R17 Arc 1 — geolocation primitives in isolation** per the planned cadence from session 117 close. David approved with *"Yes"* and the session ran as a pure execution pass against the 21-decision spec frozen in [`docs/r17-geolocation-design.md`](docs/r17-geolocation-design.md). Zero design calls, zero reversals, zero re-scoping mid-session.

**6 runtime commits sequenced smallest→largest** per `feedback_smallest_to_largest_commit_sequencing` (cumulative now >99 firings; promoted-via-memory at session 88):

| # | Commit | What |
|---|--------|------|
| 1 | `ad8c19c` | `lib/distance.ts` — pure haversine helper. `milesFromUser({lat,lng}, mallLat, mallLng): number \| null`. Earth radius 3958.8mi, 1-decimal precision (D12), null-passthrough on missing inputs (D15). 48 lines. |
| 2 | `8aef161` | `lib/mapsDeepLink.ts` — pure UA-detection + URL builder. iOS → `maps://?daddr=<lat>,<lng>&dirflg=d` (Apple Maps driving); Android+desktop → Google Maps web URL (D16). iPad-on-iOS-13+ caught via MacIntel + maxTouchPoints check. SSR-safe. 48 lines. |
| 3 | `5ebb15e` | `lib/useUserLocation.ts` hook + 5 R17 event types added to `clientEvents.ts`. Silent first-mount prompt + 30-min TTL cache + cross-instance custom-event sync + cross-tab storage-event sync (D3 + D14 + D20). Module-scope `inFlight` flag coalesces concurrent first-mounts. State machine: `idle → prompting → granted \| denied \| unavailable`. Denied is cached with same TTL so we don't re-prompt 14 times in a session after one denial. 5 R17 events: `location_prompted` / `location_granted` / `location_denied` / `find_navigate_tapped` / `find_view_on_map_tapped` (D21). |
| 4 | `a87de86` | `<DistancePill>` primitive — pure presentational, renders nothing on null. D6 visual lock: paper-warm `#ede6d5` bg, 1px ink-hairline border, 999px radius, 3×10 padding, FONT_SYS 10px/700/0.12em letter-spacing/uppercase, ink-muted color. D7 copy: `"X.Y MI"` minimal — Frame α. 55 lines. |
| 5 | `ef96800` | `<LocationActions>` primitive — twin-button row composing `useUserLocation` + `navigateUrl` + `track()`. Hides entirely when mall coords missing OR `useUserLocation().status !== "granted"` (D4 silent failure + D15 defensive null-check). Outline "View on Find Map" (Lucide Map glyph) routes to `/map?mall=<slug>` (D17); filled green "Navigate" (Lucide Navigation glyph) sets `window.location.href` to platform deep-link (D9 + D10 + D18). 136 lines. |
| 6 | `c58b3be` | `/geolocation-test` smoke page mounting all primitives in isolation. Mirrors `/postcard-test` (R10 session 107) + `/search-bar-test` (R16 session 102) per `feedback_testbed_first_for_ai_unknowns`. Sections: live `useUserLocation` status display, `<DistancePill>` across 5 distance values + null slot, standalone `<LocationActions>`, integrated find-card eyebrow-row mock, UA + geolocation-availability env diagnostics, "Clear cache + re-prompt" button. 307 lines. |

**Build clean at every commit boundary.** `npx tsc --noEmit` ran clean after each file write; `npm run build` ran clean before push, with `/geolocation-test` appearing as a static route.

**David's iPhone QA on Vercel preview against `/geolocation-test`: clean** — single-word verdict means prompt fired on first mount, status flipped `prompting → granted`, DistancePill rendered correctly across all values + null slot rendered nothing, twin CTAs rendered below the integrated find-card mock, Navigate opened Apple Maps with driving directions to America's Antique Mall, "View on Find Map" routed to `/map?mall=americas-antique-mall`, cache-clear + re-prompt cycle worked.

**Stopping point: Arc 1 lock.** Per the session 117 close opener — *"After Arc 1 push: David runs iPhone QA on Vercel preview against the smoke page. If clean → Arc 2 (consumer wiring) becomes the next-session move."* — Claude offered (a) close per planned cadence vs (b) push into Arc 2 in same session. Recommended (a) — Arc 2 touches 4 production surfaces with active consumers, better as its own session with fresh attention + dedicated iPhone QA on real seeded data. David picked close.

**Memory firings this session:**

- **`feedback_smallest_to_largest_commit_sequencing`** — 6 commits canonically ordered. Cumulative >99 firings (was 93 at session 116 close). Promoted-via-memory at session 88; this session is yet another validation of the rule's load-bearing role in shipping discipline.
- **`feedback_testbed_first_for_ai_unknowns`** — `/geolocation-test` first, before any consumer wiring. Pattern is now 3-firings deep across multi-surface primitive ships: `/postcard-test` (R10 session 107) + `/search-bar-test` (R16 session 102) + `/geolocation-test` (R17 session 118). The shape — primitive validation in isolation against real production background, before touching live surfaces — is a project canonical now.
- **`feedback_browser_api_in_useState_initializer_hydration`** — `useUserLocation` defaulted to `INITIAL = { status: "idle", lat: null, lng: null, capturedAt: null }` rather than reading from localStorage in the initializer; cache read happens in useEffect. Defensive (no actual hydration bug surfaced — pattern honored proactively).
- **`feedback_treehouse_no_coauthored_footer`** — honored on all 6 commits.

**NEW pattern (single firing, Tech Rule candidate on second firing):**

- *"Design-record-as-execution-spec — implementation runs against frozen decisions without re-scoping."* 21 decisions D1–D21 from session 117 ran cleanly through Arc 1; zero design calls, zero mid-session re-scoping, zero design-record reversals. Cleanest realization of the session-56 design-record-as-commitment-ceremony rule since R16 Task 4 (session 102). Implication for future Arc planning: a fully-scoped design record reduces implementation cost by eliminating decision-overhead; if a future Arc 1 surfaces design re-scoping, it's a signal the design pass left axes open. Tech Rule on second firing.

**Implementation-time decisions inside locked axes (NOT design reversals):**

- `<LocationActions>` analytics payload uses `mall_slug` not `mall_id`. D21 said `mall_id`; the consumer-surface canonical key is the slug (powers `/map?mall=<slug>` routing + URL state from R10). Analytics-layer can resolve slug→id later if R3 consumers expect `mall_id`. Trivial one-line rename if it matters.
- `useUserLocation` caches *denied* status with same 30-min TTL as granted. D14 said "TTL of 30 minutes" for cached coords without specifying denied behavior. Reasoning: re-prompting 14 times per session as the user hits Map → /shelf/[slug] → /find/[id] → back is worse UX than 30-min retry latency. If telemetry shows users want faster retry, a "request re-prompt" affordance ships with deferred permission-promotion UI.
- `<LocationActions>` uses `window.location.href = navigateUrl(...)` not `router.push(...)` for Navigate. `router.push` can't handle the non-http `maps://` scheme.

**Net runtime change:** +600 lines across 6 new files; only existing-file edit is the 5-line type-union extension in `clientEvents.ts`. All new code is opt-in — no production consumer mounts the hook or primitives until Arc 2; the smoke-test page is the only consumer today, gated to `/geolocation-test`.

**Roadmap:** unchanged at row level. **17 R-rows total. 10 ✅ Shipped, 2 🟢 Ready (R3, R17 — Arc 1 of 2 shipped), 5 🟡 Captured.** R17 graduates ✅ Shipped only after Arc 2 wires consumers + R3 events fire on the live surfaces.

**Operational follow-ups (carry into session 119):**

- 🚧 **R17 Arc 2 — consumer wiring** (~1 session, ~5 commits smallest→largest) — wire `<DistancePill>` + `<LocationActions>` into `/find/[id]` cartographic eyebrow row + `/shelf/[slug]` BoothHero + `<BoothLockupCard>` extension for `/flagged` + `<PinCallout>` extension for `/map`. R3 events fire automatically through the LocationActions primitive (already wired in commit 5).
- 🚧 **CARRY (NEW 116→119): Tier C polish bundle** (~half-session, 3 items) — back button on /flagged + heart icon flicker on Home + Saved badge flicker on BottomNav. Items 2+3 share root cause (`useShopperSaves` async hydration). Was recommended at session 117 + 118 open; deferred for R17 design + Arc 1 implementation. Now 4 sessions deep on this carry.
- 🚧 **CARRY (110→119): Home masonry right-column animation regression** — needs reproduction; David capture video on next walk.
- 🚧 **R3 implementation** — only other 🟢 Ready row. Foundation for R5b tuning + future feed ranking.
- 🚧 **CARRY (108→119): Italic-Lora label face on Mapbox basemap via Studio** (~half-day).
- 🚧 **CARRY (115→119): `/login/email` alias retirement** — D6-locked at ~30-day window post-2026-06-04. Watch session 119+ for the trigger date.

**Commits this session (6 runtime + 1 close commit):**

| Phase | Commit | Message |
|---|---|---|
| 1 | `ad8c19c` | feat(r17): lib/distance.ts haversine helper |
| 2 | `8aef161` | feat(r17): lib/mapsDeepLink.ts native-maps URL builder |
| 3 | `5ebb15e` | feat(r17): useUserLocation hook + 5 R17 event types |
| 4 | `a87de86` | feat(r17): &lt;DistancePill&gt; primitive |
| 5 | `ef96800` | feat(r17): &lt;LocationActions&gt; primitive |
| 6 | `c58b3be` | feat(r17): /geolocation-test smoke page |
| (close) | _this commit_ | docs: session 118 close — R17 Arc 1 primitives shipped + clean iPhone QA |

---

## ✅ Session 117 (2026-05-06) — R17 geolocation design pass + design-to-Ready (🟡 → 🟢 same-session) — 1 design-record commit + close (rotated to mini-block session 118 close)

> Full block rotated out at session 118 close. Net: 1 design-record commit `87f7a43` (+1650/−1) closing 21 decisions D1–D21. David opened with /session-open recommending Tier C polish bundle; redirected to product+design session on geolocation enabling with reference mockup. Reference-first scan of 6 cross-domain references (AllTrails / Apple Maps / Yelp / DoorDash / Facebook Marketplace / Eventbrite) before any HTML mockup. V1 (3 structural frames spanning pill anchor + CTA shape + permission-denied state axes) → David's hybrid pick "Frame A but with the distance pill shape of Frame B" → V2 (Frame A's twin-button CTAs + Frame B's eyebrow-row pill anchor; 3 fill-refinement frames spanning distance copy axis only) → David's V2 pick: Frame α "2.7 MI" minimal — postmark stamp, no API dependency. Initial V2 interpretation drafted with anchor A + typography B; David interrupted mid-draft with correct interpretation anchor B + CTAs A. NEW MEMORY captured: `feedback_user_clarification_restate_interpretation.md` — when user picks "Frame X but with element from Frame Y," restate the interpretation as one explicit sentence before drafting next iteration. R17 graduates 🟡 → 🟢 same-session — second R-row to graduate same-day after R16 (session 102), confirming the reference-first → V1 (structural axes) → V2 (fill-refinement axes only) chain is a 5-firing repeatable pattern. `feedback_reference_first_when_concept_unclear` 5th cumulative firing across sessions 99/102/111/115/117 — promotion-strength validated repeatedly. `feedback_v2_mockup_as_fill_refinement` 3rd cumulative firing (promotion-ready). Implementation infrastructure verified: all 29 active malls have lat/lng populated; /map?mall=<slug> from R10 (session 108) accepts mall scope; BoothLockupCard + BoothHero + PinCallout primitives augmentable without breaking changes. 21 decisions D1–D21 frozen in design record. Roadmap: 17 R-rows total (was 16). 10 ✅ Shipped, 2 🟢 Ready (R3, R17 NEW), 5 🟡 Captured.

_(Session 117 detailed beat narrative removed at session 118 close — see git history at commit `65c913b` for the full beat-by-beat narrative.)_

---

## ✅ Session 116 (2026-05-06) — 4-item polish bundle CLEARED + PostcardMallCard iPhone-QA refinement + dormant /shelves retired — 11 runtime commits + close (rotated to mini-block session 117 close)

> Full block rotated out at session 117 close. Net: 11 runtime commits across 7 files (+103 / −737, ~634 net delete with /shelves retirement). David opened with /session-open recommending the long-carrying 4-item polish bundle (4 sessions deep on carries 108→110→113→115→116); review surfaced an additional 8-item iPhone-QA list. Tiered triage (A pure-dial / B one-design-call / C structural-defer) kept the session focused as a single ship: Tier A + B + original 4 items in scope; Tier C (back button on /flagged + heart flicker + badge flicker) deferred to session 118. 9-commit smallest→largest plan: `36714b0` MallSheet em-dash; `04ee27d` inspect-rls.ts shopper_* tables; `56af464` SearchBar copy; `9308075` cancellation color v1.inkPrimary → v1.inkMid; `a46437c` cancellation 50% narrower; `13e9fbb` PostcardMallCard fluid font-size measure-and-shrink (constant 84px card height); `703e21c` SELECT LOCATION eyebrow above stamp; `3fd1031` share button on /flagged masthead right slot (bounded reversal of session-109 D1 — URL only encodes ?mall=<slug> scope, never save list); `cf52976` retire dormant /shelves page (−701 lines). Mid-session iPhone QA on Vercel preview produced 2 follow-up commits with David's verbatim values: `a730f13` cancellation revisions per iPhone QA (width 84/right:14/opacity 0.08/v1.inkPrimary stroke); `1ba4df5` eyebrow refactor "SELECT LOCATION" → "Location" (FONT_LORA italic 11px right-aligned, mirrors same-card "from:" eyebrow voice). 3 NEW patterns (single firings, all Tech Rule candidates on second firing): "user-provided verbatim values ship as-is"; "eyebrow vocabulary should match same-card existing voice"; "tier the ask before kicking off when scope expands mid-conversation." `feedback_smallest_to_largest_commit_sequencing` cumulative >93. `feedback_mid_session_iphone_qa_on_vercel_preview` ~15th firing. `feedback_surface_locked_design_reversals` ~20th cumulative firing. `feedback_lora_lineheight_minimum_for_clamp` 4th firing. Roadmap unchanged at row level (UX polish, not feature ship). Original 4-item polish bundle CLEARED — operational backlog clears one of its longest-running carries.

_(Session 116 detailed beat narrative removed at session 117 close — see git history at commit `87f7a43` for the full beat-by-beat narrative.)_

---

## ✅ Session 115 (2026-05-05) — Login email-first redesign (Shape A) + admin-without-booth stranding fix end-to-end — 4 runtime commits + 1 docs commit + close (rotated to mini-block session 116 close)

> Full block rotated out at session 116 close. Net: 4 runtime commits + 1 docs commit. David opened with /session-open recommending 4-item polish bundle; redirected to product + design session on login triage. Reference-first scan of cross-domain triage patterns (Slack/Notion/Substack/Apple iCloud/Etsy/Airbnb) → 4 candidate shapes (Shape A email-first / B two-tier / C reframe-current / D defer-entirely). David picked Shape A. V1 mockup spanned 3 structural frames (Frame A Notion-pure / Frame B email + visible vendor card / Frame C none-state landing); David picked Frame A + Frame C. V2 locked all 12 decisions D1–D12 with David's verbatim copy. **Net runtime code: +197 / −983.** /login/email's 800-line OTP form moved verbatim into /login; /login/email retired to a 33-line redirect alias preserving query params for ~30 days post-2026-06-04. The codebase shrinks while the feature improves: one email field replaces three triage cards, server detects who you are via detectUserRole, fresh-auth users with no rows land on a new /welcome disambiguation card (vendor or shopper, first-sign-in only). Side-effect: Arc 1 fixes admin-without-booth stranded-state on /login (David's typical config). D3 (Apple/Google SSO) explored + deferred — Frame A's layout has natural headroom above email card for additive SSO; double-work risk LOW. NEW patterns (single firings, all Tech Rule candidates on second firing): "folding multi-route flows into one route preserves working code"; "defer external-dep features with explicit scope-locked headroom in design"; "user-facing copy lexicon scrub — flag introduced new terms before lock." `feedback_reference_first_when_concept_unclear` 4th cumulative firing across sessions 99/102/111/115 — promotion-strength validated repeatedly. `feedback_verify_role_conditional_doesnt_strand_non_canonical_role_holders` 2nd firing → Tech Rule promotion-ready on third. Routing matrix: admin → /, vendor → /my-shelf, shopper → /me, **none → /welcome (NEW)** replacing /my-shelf fall-through stranded state.

_(Session 115 detailed beat narrative removed at session 116 close — see git history at commit `16fcb89` for the full beat-by-beat narrative.)_

---

## ✅ Session 114 (2026-05-05) — R1 Arc 5 re-walk passed + BottomNav rightmost role-tab + Admin variant — 1 runtime commit + R1 ✅ Shipped + close (rotated to mini-block session 115 close)

> Full block rotated out at session 115 close. Net: 1 runtime commit `cc06ae9` (+40/−15) graduating R1 ✅ Shipped end-to-end. David ran 5-step re-verification walk on deployed `5cea49a`. Steps 1–4 passed; Step 5 partial-pass surfaced two redirects (admin-without-booth stranded + tab order middle → rightmost). Per `feedback_surface_locked_design_reversals` (19th firing), surfaced session 113's locked tab-order reasoning verbatim before drafting code. Two-axis redirect: (1) tab order middle → rightmost (Saved holds stable slot 2 across role transitions); (2) Admin variant with Shield glyph from lucide-react (admin precedence wins over vendor per Option A). 6th iteration of R10 D1: Guest 3-tab → Vendor 4-tab → flat 4-tab → flat 3-tab → flat 2-tab → 2-or-3-tab role-conditional middle-tab → 2-or-3-tab role-conditional rightmost-tab + Admin variant. R1 graduates ✅ Shipped — 10/15 R-items shipped. NEW patterns (single firings): "stable persistent tabs in nav, not role-dependent positions"; "verify role-conditional UI doesn't strand non-canonical role holders" (the latter caught its 2nd firing at session 115 fixing admin-without-booth on /login).

_(Session 114 detailed beat narrative removed at session 115 close — see git history at commit `c4dd986` for the full beat-by-beat narrative.)_

---

## Recent session tombstones (last 5)

> Older tombstones live in [`docs/session-archive.md`](docs/session-archive.md). Sessions 28–43 still awaiting archive-drift backfill (one-liner only); sessions 44–53 + 56 in archive (54 + 55 + 57–110 still missing — operational backlog growing). Session 113 mini-block rotated off at session 118 close. Visible window now sessions 114 mini + 115 mini + 116 mini + 117 mini + 118 full.

- **Session 118** (2026-05-06) — R17 Arc 1: geolocation primitives shipped in isolation + clean iPhone QA + close: **6 runtime commits sequenced smallest→largest, all clean tsc + npm run build at every commit boundary.** David opened with /session-open recommending R17 Arc 1 per the planned cadence from session 117 close; David approved with "Yes" and the session ran as a pure execution pass against the 21-decision spec frozen in `docs/r17-geolocation-design.md`. Zero design calls, zero reversals, zero re-scoping mid-session. Commits: `ad8c19c` lib/distance.ts haversine helper (1-decimal precision, null-passthrough); `8aef161` lib/mapsDeepLink.ts native-maps URL builder (iOS Apple Maps `maps://` + Android/desktop Google Maps web; iPad-on-iOS-13+ caught via MacIntel + maxTouchPoints; SSR-safe); `5ebb15e` useUserLocation hook + 5 R17 event types added to clientEvents.ts (silent first-mount prompt + 30-min TTL cache + cross-instance custom-event sync + cross-tab storage-event sync; module-scope inFlight flag coalesces concurrent first-mounts; state machine idle → prompting → granted | denied | unavailable; denied cached with same TTL); `a87de86` <DistancePill> primitive (D6 visual lock — paper-warm bg #ede6d5, 1px ink-hairline border, 999px radius, FONT_SYS 10px/700/0.12em letter-spacing/uppercase, ink-muted color; D7 copy "X.Y MI" minimal Frame α; renders nothing on null); `ef96800` <LocationActions> primitive (twin-button row composing useUserLocation + navigateUrl + track; hides entirely when mall coords missing OR status !== granted per D4 + D15; outline View on Find Map routes /map?mall=<slug>, filled green Navigate sets window.location.href to platform deep-link); `c58b3be` /geolocation-test smoke page (mirrors /postcard-test R10 + /search-bar-test R16 per `feedback_testbed_first_for_ai_unknowns` — 3-firings deep now). David's iPhone QA on Vercel preview against /geolocation-test: clean — single-word verdict means prompt fired on first mount, status flipped prompting → granted, DistancePill rendered correctly across all distance values + null slot rendered nothing, twin CTAs rendered below the integrated find-card mock, Navigate opened Apple Maps with driving directions to America's Antique Mall, View on Find Map routed to /map?mall=americas-antique-mall, cache-clear + re-prompt cycle worked. Stopping point Arc 1 lock: per session 117 close opener "if clean → Arc 2 (consumer wiring) becomes the next-session move," Claude offered (a) close per planned cadence vs (b) push into Arc 2 same session; David picked close (Arc 2 touches 4 production surfaces — better as its own session with fresh attention). NEW pattern (single firing, Tech Rule candidate on second firing): **"Design-record-as-execution-spec — implementation runs against frozen decisions without re-scoping."** 21 decisions D1–D21 from session 117 ran cleanly through Arc 1 with zero design calls or mid-session re-scoping — cleanest realization of the session-56 design-record-as-commitment-ceremony rule since R16 Task 4 (session 102). Implication for future Arc planning: a fully-scoped design record reduces implementation cost by eliminating decision-overhead; if a future Arc 1 surfaces design re-scoping, it's a signal the design pass left axes open. Implementation-time decisions inside locked axes (NOT design reversals): mall_slug not mall_id in analytics payload (slug is consumer-surface canonical key); denied status cached with same 30-min TTL as granted (don't re-prompt 14 times per session after one denial); window.location.href not router.push for Navigate (router.push can't handle non-http maps:// scheme). Memory firings: `feedback_smallest_to_largest_commit_sequencing` cumulative >99 firings (was 93); `feedback_testbed_first_for_ai_unknowns` 3-firings deep across primitive ships; `feedback_browser_api_in_useState_initializer_hydration` honored proactively (hook defaults to INITIAL idle state, cache read in useEffect); `feedback_treehouse_no_coauthored_footer` honored on all 6 commits. Net runtime change: +600 lines across 6 new files; only existing-file edit is 5-line type-union extension in clientEvents.ts. All new code is opt-in — no production consumer mounts hook or primitives until Arc 2; smoke-test page is the only consumer today. Roadmap unchanged at row level: 17 R-rows, 10 ✅ Shipped, 2 🟢 Ready (R3 + R17 — Arc 1 of 2 shipped), 5 🟡 Captured. R17 graduates ✅ Shipped only after Arc 2 wires consumers + R3 events fire on live surfaces. Net change toward investor narrative: the design-record-as-execution-spec pattern compounds with the reference-first → V1 → V2 design-pass shape — sessions 117 + 118 together demonstrate "spec it once thoroughly, ship it cleanly without re-scoping" as a repeatable 2-session arc that converts unclear-shape feature asks into shipping primitives in ~2 sessions, every time. The compounding investor shape continues: each R-item closure stacks onto a project posture where the design philosophy is articulated, the operational disciplines are memory-backed, and the codebase grows in capability with disciplined per-session scope.
- **Session 117** (2026-05-06) — R17 geolocation design pass + design-to-Ready (🟡 Captured → 🟢 Ready, same-session — 2nd R-row to graduate same-day after R16) + close: 1 design-record commit `87f7a43` (+1650/−1) closing 21 decisions D1–D21. David opened with /session-open recommending Tier C polish bundle; redirected to product+design session on geolocation enabling. Reference-first scan of 6 cross-domain references (AllTrails / Apple Maps / Yelp / DoorDash / Facebook Marketplace / Eventbrite) before any HTML mockup. V1 (3 structural frames spanning pill anchor + CTA shape + permission-denied state axes) → David's hybrid pick "Frame A but with the distance pill shape of Frame B" → V2 (Frame A's twin-button CTAs + Frame B's eyebrow-row pill anchor; 3 fill-refinement frames spanning distance copy axis only) → David's V2 pick: Frame α "2.7 MI" minimal — postmark stamp, no API dependency. Implementation sequenced in design record: Arc 1 (one session, 6 commits) primitives in isolation — lib/distance.ts haversine + lib/mapsDeepLink.ts UA-detection + useUserLocation hook silent first-mount prompt + 30-min TTL cache + DistancePill + LocationActions + /geolocation-test smoke page; Arc 2 (one session) wire 5 consumers + R3 analytics events. Implementation infrastructure verified: all 29 active malls have lat/lng populated (session 103 `add-mall.ts` reverse-geocodes); `/map?mall=<slug>` from R10 (session 108) accepts mall scope; BoothLockupCard + BoothHero + PinCallout primitives augmentable without breaking changes. NEW pattern surfaced + captured as memory file (single firing, Tech Rule candidate on second firing): **"User-clarification mid-mockup-draft surfaces hidden interpretive gap — restate before continuing"** ([`feedback_user_clarification_restate_interpretation.md`](memory/feedback_user_clarification_restate_interpretation.md)). Caught when initial V2 interpretation of David's "Frame A but with Frame B's pill shape" was anchor A + typography B; David interrupted mid-draft with correct interpretation anchor B + CTAs A. Memory firings: `feedback_reference_first_when_concept_unclear` 5th cumulative firing across 5 sessions (promotion-strength repeatedly validated); `feedback_v2_mockup_as_fill_refinement` 3rd cumulative firing (promotion-ready); `feedback_mockup_options_span_structural_axes` (V1 frames spanned genuinely different shapes); `feedback_visual_reference_enumerate_candidates` (David's reference mockup treated as capability-level guidance not pixel-level spec). Roadmap: 17 R-rows total (was 16). 10 ✅ Shipped, 2 🟢 Ready (R3, **R17 NEW**), 5 🟡 Captured. Net change toward investor narrative: digital-to-physical bridge thesis sharpens further — R17 is the first feature where the digital surface measures the physical bridge (distance + native-maps deep-link is the most concrete possible "go visit this real place" affordance), not just describes it. Reference-first → V1 (structural axes) → V2 (fill-refinement axes only) chain is now a 5-firing repeatable pattern; design-record-as-commitment-ceremony per session-56 rule continues; roadmap rows promote 🟡 → 🟢 same-session when reference + 2-mockup chain is sufficient (R16 + R17 both demonstrate this).
- **Session 116** (2026-05-06) — 4-item polish bundle CLEARED (4 sessions deep on carries 108→110→113→115→116) + PostcardMallCard iPhone-QA refinement + dormant /shelves retired + close: 11 runtime commits across 7 files. David opened with /session-open recommending the long-carrying 4-item polish bundle; review surfaced an additional 8-item list from his own iPhone-QA observations. Tiered triage (A pure-dial / B one-design-call / C structural-defer) kept the session focused: Tier A (3 items) + B (2 items) + original 4-item bundle in scope; Tier C (back button on /flagged + heart flicker + badge flicker) deferred to next session. 9-commit smallest→largest plan: `36714b0` MallSheet em-dash removed; `04ee27d` inspect-rls.ts TABLES[] for shopper_* tables; `56af464` SearchBar placeholder → "Search to discover local finds"; `9308075` cancellation color v1.inkPrimary → v1.inkMid; `a46437c` cancellation 50% narrower (paths halved + paddingRight match); `13e9fbb` PostcardMallCard fluid font-size measure-and-shrink locks card to constant height; `703e21c` SELECT LOCATION eyebrow above stamp on interactive cards; `3fd1031` share button on /flagged masthead right slot (bounded reversal of session-109 D1 — shared URL only encodes ?mall=<slug> scope, never save list); `cf52976` retire dormant /shelves page (−701 lines, 301 redirect at next.config.js intercepts cached bookmarks). Mid-session iPhone QA on Vercel preview produced 2 follow-up commits: `a730f13` cancellation revisions per David's verbatim SVG (width 84/right:14/opacity 0.08/v1.inkPrimary stroke — heavy overlap with stamp body reads as watermark texture not competing element); `1ba4df5` eyebrow refactor "SELECT LOCATION" → "Location" (FONT_LORA italic 11px right-aligned, mirrors the same-card "from:" eyebrow vocabulary instead of generic system-wide small-caps). **Net runtime change: +103 / −737 across 7 files** — codebase shrinks ~634 lines (mostly /shelves retirement). Side-effect doc fixes: stale "/shelves as primary booth-bookmarking surface" comment in `app/(tabs)/flagged/page.tsx` updated to /shelf/[slug] (correct since session 80); `app/(tabs)/layout.tsx` share-right-slot comment block updated to reflect /flagged share reversal + bounded-payload reasoning. NEW patterns (single firings, all Tech Rule candidates on second firing): "user-provided verbatim values (SVG, copy, code) ship as-is — don't relitigate the spec" (David's cancellation SVG shipped verbatim with comment block noting iPhone-QA-derived dial; saves a round of spec-writing); "eyebrow vocabulary should match same-card existing voice" (the small-caps FONT_SYS pattern was the wrong system-wide pull; the right reach was THIS-CARD's existing italic "from:" eyebrow); "tier the ask before kicking off when scope expands mid-conversation" (12 items would have been a 90-min session-half-start; A/B/C triage with C deferred kept the session a single ship). `feedback_smallest_to_largest_commit_sequencing` cumulative >93. `feedback_mid_session_iphone_qa_on_vercel_preview` ~15th firing. `feedback_surface_locked_design_reversals` ~20th cumulative firing across 8+ sessions. `feedback_lora_lineheight_minimum_for_clamp` 4th firing (preserved during fluid font-size impl). Roadmap unchanged at row level — UX polish, not feature ship. **10 ✅ Shipped, 1 🟢 Ready (R3), 5 🟡 Captured.** Original 4-item polish bundle CLEARED — operational backlog clears one of its longest-running carries.
- **Session 115** (2026-05-05) — Login email-first redesign (Shape A) + admin-without-booth stranding fix end-to-end + close: 4 runtime commits + 1 docs commit. David opened with /session-open recommending 4-item polish bundle; redirected to product + design session on login triage. Reference-first scan of cross-domain triage patterns (Slack/Notion/Substack/Apple iCloud/Etsy/Airbnb) → 4 candidate shapes (Shape A email-first / B two-tier / C reframe-current / D defer-entirely). David picked Shape A. V1 mockup spanned 3 structural frames (Frame A Notion-pure / Frame B email + visible vendor card / Frame C none-state landing); David picked Frame A + Frame C. V2 locked all 12 decisions D1–D12 with David's verbatim copy. D3 (Apple/Google SSO) explored + deferred — Frame A's layout has natural headroom for additive SSO; double-work risk LOW. D11 lexicon flag: "digital storefront" → "digital booth" (project canonical). D12 third row dropped + 2nd row reframed: David picked binary welcome card (vendor or shopper) + first-sign-in only. 4-arc implementation smallest→isolated first: Arc 3 `3b861f5` (+13) /vendor-request email pre-fill from authed user; Arc 2 `e0fb3d0` (+293) /welcome first-sign-in disambiguation card; Arc 1 `3440c2d` (+672/−160) email-first /login + pickDest none → /welcome branch; Arc 4 `c8b5a93` (+15/−823) /login/email redirect alias to /login. Docs `14dbce2` (+1339) login email-first V1 + V2 mockups. **Net runtime code: +197 / −983** — codebase shrinks while feature improves. Side-effect bug fix: Arc 1 closes admin-without-booth stranded-state on /login (David's typical config) — action cards now query vendors row separately; admin-without-booth sees only "Admin tools" card. `feedback_verify_role_conditional_doesnt_strand_non_canonical_role_holders` 2nd firing → Tech Rule promotion-ready. /login/email kept as redirect alias for ~30 days post-2026-06-04. David ran iPhone QA on Vercel preview after push: all passed. NEW patterns (single firings, all Tech Rule candidates on second firing): "folding multi-route flows into one route preserves working code" (700 LOC of OTP machinery moved verbatim from /login/email into /login; net runtime change −983/+197 means codebase shrinks while feature improves); "defer external-dep features with explicit scope-locked headroom in design" (D3 SSO deferred but Frame A layout has natural slot above email card for additive SSO without redesign); "user-facing copy lexicon scrub — flag introduced new terms before lock" (D11 sub-pattern of existing user-facing copy scrub rule, applied to fresh David-supplied copy not historic identifiers). `feedback_reference_first_when_concept_unclear` 4th cumulative firing across sessions 99/102/111/115 — promotion-strength validated. Roadmap unchanged at row level (UX refactor not feature ship). /login surface stabilizes after 4 prior touches (49/90/93/113/114) + this session's consolidating refactor. Routing matrix updated: admin → /, vendor → /my-shelf, shopper → /me, **none → /welcome (NEW)**.
- **Session 114** (2026-05-05) — R1 Arc 5 re-walk passed + BottomNav rightmost role-tab + Admin variant + close: 1 runtime commit graduates R1 ✅ Shipped end-to-end. David ran the 5-step re-verification walk on deployed `5cea49a`. Steps 1-4 passed clean; Step 5 partial-pass with redirect — David, signed in as admin without a vendor booth, hit "No booth assigned" on /my-shelf with no escape AND no /admin reachability from the (tabs) routes. The Booth tab session 113 shipped for vendor + admin had stranded the non-canonical (admin-without-booth) configuration David himself runs in. David also redirected on tab order: middle → rightmost. Per `feedback_surface_locked_design_reversals` (19th cumulative firing across 7+ sessions), surfaced session 113 D1 5th-iteration reasoning verbatim before drafting code. Two-axis redirect proposed + accepted: (1) tab order middle → rightmost (Saved holds stable 2nd position for muscle memory across role transitions); (2) Admin variant with Shield glyph from lucide-react (matches /admin/login chrome; routes to /admin instead of /my-shelf). Edge case asked + answered: admin + vendor (David's typical config) — admin precedence wins (Option A). detectUserRole already returns "admin" before "vendor"; admins navigate to /my-shelf via /admin or directly when they happen to also have a booth. **1 runtime commit `cc06ae9`** (+40/−15): BottomNav rightmost role-tab + Admin variant; tab order Home → Saved → [Booth \| Admin]. 6th iteration of R10 D1 — Guest 3-tab → Vendor 4-tab → flat 4-tab → flat 3-tab → flat 2-tab → 2-or-3-tab role-conditional middle-tab → 2-or-3-tab role-conditional rightmost-tab + Admin variant. **R1 graduates 🟢 Ready → ✅ Shipped.** 5 arcs across 4 sessions: design + Arcs 1–3 (111) → Arc 4 hybrid hooks (112) → Arc 5 walk + RLS partial-paste fix + 3-commit fix bundle (113) → Arc 5 re-walk passed + BottomNav refinement (114). 10/15 R-items shipped now. NEW patterns (single firings, all Tech Rule candidates on second firing): "stable persistent tabs in nav, not role-dependent positions" (Saved sits at slot 2 in every variant; the role-specialty slot is rightmost regardless of which role unlocks it; generalizes beyond R10 D1 to any future role-aware nav surface — `/admin` chrome, role-aware mastheads, future R13 mall-operator nav); "verify role-conditional UI doesn't strand non-canonical role holders" (caught 2nd firing at session 115 fixing admin-without-booth on /login).
- **Session 113** (2026-05-05) — R1 Arc 5 end-to-end QA walk + RLS partial-paste fix + 3-commit follow-up bundle: 3 runtime commits + close. David ran the 4-phase QA walk; Phase 1 cold-start passed; Phase 2 step 9 BLOCKED — handle submit silently failed. Per `feedback_visibility_tools_first` + `feedback_cap_speculative_patching_at_3_rounds`, escalated to HAR via Safari Web Inspector at Round 1 instead of patching theories. HAR showed POST /api/shopper-claim → 200 (row created via service role) but every authenticated SELECT returned `[]`. Root cause via `audit_rls_state()` RPC: migration 020 partial-paste — `shoppers / shopper_saves / shopper_booth_bookmarks` all had `rls_enabled: true / policy_count: 0` on BOTH prod + staging. RLS-on with 0-policies = silent default-deny. CLAUDE.md session 111 close said David pasted; the CREATE POLICY block must have failed silently in the SQL editor on both runs. HITL re-paste of policy block → `1/3/3` policy counts confirmed via diagnostic + verification query. Phases 2–4 then passed. David flagged 3 issues from the walk: (1) signOut from /me lands on /login not /; (2) sign-in with shopper email lands on /my-shelf "No booth assigned"; (3) Booth nav-bar icon missing for vendor/admin. Per `feedback_surface_locked_design_reversals` (cumulative >18 firings) on issue 3, surfaced session 109+110 D1 lock + reasoning + presented 3 paths; David picked Path 2 (reverse with role-conditional Booth tab). 3 runtime commits smallest→largest: `6a17ab2` /me redirect destination /login → / (5 lines); `2b56f40` async pickDest + new `detectUserRole(user)` helper in lib/auth.ts (queries shoppers + vendors in parallel, returns 'admin' | 'vendor' | 'shopper' | 'none'; 7 callsites awaited across both /login + /login/email; shopper-only users now route to /); `5cea49a` role-conditional Booth tab in BottomNav (vendor + admin only, 5th iteration of R10 D1 design log; consumed via onAuthChange reactively; tab order Home → Booth → Saved). NEW patterns (single firings, all Tech Rule candidates on second firing): "migration partial-paste failure mode (RLS-on + 0-policies is silent shape — verify via policy_count not just relrowsecurity)"; "service-role INSERT + RLS-denied authenticated SELECT = post-INSERT row invisible to client"; "async pickDest pattern via shared role-detection primitive (detectUserRole reusable for role-aware chrome)"; "role-conditional tab insertion via spread operator"; "standup error: misread of prior-session close block creates wrong expectation in QA walk." End-of-session: `5cea49a` deployed; awaiting David's 5-step re-verification walk on the deployed surface (signOut → /, sign-in shopper → /, sign-in vendor/admin → /my-shelf with 3-tab BottomNav, sign-out reverts to 2-tab reactively, Booth tap one-tap). R1 graduates ✅ Shipped on clean walk.
_(Session 113 tombstone rotated off at session 118 close — see git history at commit `65c913b` for the full session-113 tombstone narrative. Session 112 rotated off at session 117 close — see git history at commit `c9b90d3`. Session 111 rotated off at session 116 close — see git history at commit `16fcb89`. Session 109+110 rotated off at session 115 close — see git history at commit `c4dd986`. Session 108 rotated off at session 114 close — see git history at commit `46890e6`. Sessions 87–113 missing from `docs/session-archive.md`; backfill remains operational backlog.)_

---

## CURRENT ISSUE
> Last updated: 2026-05-06 (session 118 close — R17 Arc 1 geolocation primitives shipped in isolation. 6 runtime commits smallest→largest, build clean at every commit boundary, David's iPhone QA on /geolocation-test passed clean. Arc 2 — consumer wiring across 5 surfaces — is the next-session move.)

**Working tree:** clean (after close commit). **Build:** green. **Beta gate:** unblocked. **Net change this session:** 6 runtime commits + 1 type-union extension; +600 lines across 6 new files. **Commits (on `main`):** `ad8c19c` distance.ts · `8aef161` mapsDeepLink.ts · `5ebb15e` useUserLocation hook + R17 events · `a87de86` <DistancePill> · `ef96800` <LocationActions> · `c58b3be` /geolocation-test smoke page.

Roadmap: **17 R-rows total. 10 ✅ Shipped (R1, R4a/b/c, R5a, R7, R10, R11, R12, R16), 2 🟢 Ready (R3, R17 — Arc 1 of 2 shipped), 5 🟡 Captured.**

### 🚧 Recommended next session — R17 Arc 2: consumer wiring across 5 surfaces (~1 session, ~5 commits smallest→largest)

Arc 1 primitives are validated. Arc 2 wires `<DistancePill>` + `<LocationActions>` into the 5 production surfaces from D18 + D19 of the design record. R3 events fire automatically through the LocationActions primitive (already wired in commit 5). Pattern matches R10 Arc 2 (session 107) + R16 implementation sprint (session 105).

**Plan (Arc 2, smallest→largest):**

1. **🟢 AUTO — `<BoothLockupCard>` extension** — accept optional `distancePill: ReactNode` prop and render above the card right-aligned. Wraps both `<DistancePill miles={...}>` and the empty-when-null behavior. Then wire on `/flagged` (BoothGroup section header).
2. **🟢 AUTO — `/find/[id]` cartographic eyebrow row** — pair existing italic Lora "Find this item at" eyebrow with `<DistancePill>` right-aligned; mount `<LocationActions surface="find" postId={id} vendorId={post.vendor_id}>` below the cartographic card.
3. **🟢 AUTO — `/shelf/[slug]` BoothHero post-it eyebrow row** — pair existing "Booth" eyebrow with `<DistancePill>` right-aligned; mount `<LocationActions surface="booth" vendorId={vendor.id}>` below BoothHero.
4. **🟢 AUTO — `<PinCallout>` extension** — accept optional `distance` prop; render in callout header next to mall name; mini "Browse" + "Go" buttons in callout body (compressed CTAs to fit chrome).
5. **🟢 AUTO — Polish + iPhone QA pass** — verify all 4 wired surfaces against real seeded data; surface any visual bugs; ship dial commits.

After Arc 2 push: David runs iPhone QA on Vercel preview against `/find/[id]` + `/shelf/[slug]` + `/flagged` + `/map`. If clean → R17 graduates ✅ Shipped (11/17 R-items). Investor-update trigger fires.

### 🚧 Operational follow-ups carrying

- **CARRY (NEW 116→119): Tier C polish bundle** (~half-session, 3 items) — back button on /flagged + heart icon flicker on Home + Saved badge flicker on BottomNav. Items 2+3 share root cause (`useShopperSaves` async hydration). Recommended at sessions 117 + 118 open; deferred for R17 design + Arc 1 implementation. **Now 4 sessions deep on this carry.**
- **Home masonry right-column animation regression** (carry from 110→119) — needs reproduction first; David capture video on next walk.
- **NEW (115→119): `/login/email` alias retirement** — D6-locked at ~30-day window post-2026-06-04. Watch session 119+ for the trigger date.
- **Italic-Lora label face on Mapbox basemap via Studio** (~half-day, carry from 108→119). Closes last D25 deferral.

### Alternative next moves (top 5)

1. **Tier C polish bundle** (~half-session, 3 items) — see carry above. 2 of 3 items share root cause; one design call.
2. **R3 implementation** — only other 🟢 Ready row. Analytics event capture; foundational for R5b tuning + future feed ranking. Design record at [`docs/r3-analytics-design.md`](docs/r3-analytics-design.md), all 6 decisions D1–D6 frozen. Bigger lift, deserves its own session.
3. **Italic-Lora label face on Mapbox basemap via Studio** (~half-day, carry from 108).
4. **Promote one of the 🟡 Captured rows to design phase** — R8 onboarding / R6 legal / R14 vendor enrichment / R5b tier shape / R13 mall-operator accounts. Each unlocks downstream work.
5. **Investor update generation** — natural trigger fires after R17 Arc 2 ships. Sustained R-item shipping cadence (R10 → R1 → R16 → R17 design + Arc 1 across 10 sessions) + the operational discipline narrative (design-record-as-execution-spec, 5-firing reference-first, 3-firing V2-fill-refinement) is a sprint-level investor beat.

Full alternatives + operational backlog in [`docs/queued-sessions.md`](docs/queued-sessions.md).

### Session 119 opener (pre-filled — R17 Arc 2 consumer wiring)

```
PROJECT: Treehouse Finds — Zen-Forged/treehouse-treasure-search — app.kentuckytreehouse.com
STACK: Next.js 14 App Router · TypeScript · Tailwind · Framer Motion · Anthropic SDK · Supabase · SerpAPI · Sentry · Vercel · Mapbox GL JS
Filesystem MCP is connected at /Users/davidbutler/Projects/treehouse-treasure-search
Read CLAUDE.md, CONTEXT.md, docs/r17-geolocation-design.md, docs/roadmap-beta-plus.md. Then run the session opening standup from MASTER_PROMPT.md.

CURRENT ISSUE: R17 Arc 2 — wire Arc 1 primitives into 5 production consumer surfaces (~1 session, ~5 commits smallest→largest). Arc 1 primitives shipped + validated in isolation at session 118 (commits ad8c19c → c58b3be). All 21 decisions D1–D21 frozen in design record. No design calls — pure execution.

Arc 2 plan smallest→largest:
(1) <BoothLockupCard> extension — accept optional distancePill: ReactNode prop, render above card right-aligned. Then wire /flagged BoothGroup section header.
(2) /find/[id] — cartographic eyebrow row pairs italic Lora "Find this item at" left + <DistancePill miles={milesFromUser(userLoc, mall.latitude, mall.longitude)}> right; mount <LocationActions surface="find" postId={id} vendorId={post.vendor_id}> below cartographic card.
(3) /shelf/[slug] — BoothHero post-it eyebrow row pairs existing "Booth" eyebrow + <DistancePill> right; mount <LocationActions surface="booth" vendorId={vendor.id}> below BoothHero.
(4) <PinCallout> extension — accept optional distance prop, render in callout header next to mall name; add mini "Browse" + "Go" buttons in callout body.
(5) Polish + iPhone QA pass — verify all 4 wired surfaces; ship dial commits if needed.

After push, David runs iPhone QA on Vercel preview. If clean → R17 graduates ✅ Shipped (11/17 R-items). Investor-update trigger fires.

DESIGN RECORD: docs/r17-geolocation-design.md is the spec. D18 anchors pill on each surface. D19 anchors CTAs on each surface. Implementation contracts spelled out.

ARC 1 ARTIFACTS (do not re-create — they're already on main): lib/distance.ts (haversine), lib/mapsDeepLink.ts (UA-aware URL builder), lib/useUserLocation.ts (single-source hook with TTL cache + cross-instance sync), components/DistancePill.tsx (D6-locked visual), components/LocationActions.tsx (twin-button row composing hook + tracks), app/geolocation-test/page.tsx (smoke page; consider retiring after Arc 2 if you don't want it in production).

PROBLEMS SESSION 118 ALREADY SOLVED: useUserLocation defaults to idle status + reads cache in useEffect (not useState initializer); module-scope inFlight flag coalesces concurrent first-mounts; denied status cached with same 30-min TTL; LocationActions analytics payload uses mall_slug not mall_id.

PROBLEMS SESSION 117 ALREADY SOLVED: 21 decisions D1–D21 frozen in design record. Reference-first scan + V1 + V2 mockup chain locked. R17 promoted to 🟢 Ready.

PROBLEMS SESSION 116 ALREADY SOLVED — don't accidentally revive: (a) PostcardMallCard mall name uses fluid font-size measure-and-shrink. (b) Cancellation values: width 84, right:14, opacity 0.08, stroke v1.inkPrimary. (c) "Location" eyebrow FONT_LORA italic 11px right-aligned. (d) /flagged masthead has share button (right slot). (e) /shelves page deleted; 301 redirect to /map. (f) inspect-rls.ts TABLES[] whitelists shopper_* tables.

PROBLEMS SESSION 115 ALREADY SOLVED: /login email-first; /login/email is a redirect alias; /welcome handles first-sign-in disambiguation; routing matrix admin → /, vendor → /my-shelf, shopper → /me, none → /welcome.

PROBLEMS SESSION 114 ALREADY SOLVED: BottomNav tab order Home → Saved → [Booth | Admin] (rightmost role-tab); detectUserRole returns admin before vendor; Shield glyph for admin.

PROBLEMS SESSION 113 ALREADY SOLVED: Migration 020 RLS policies LIVE (1/3/3 policy counts). lib/auth.ts detectUserRole canonical role primitive. /me's !isAuthed → /. pickDest async with 7 awaited callsites.

PROBLEMS SESSION 109+110 ALREADY SOLVED: Chrome lives in app/(tabs)/layout.tsx. Profile at masthead-left. BottomNav role-conditional. /map reached via PostcardMallCard. PostcardMallCard renders <div> when onTap undefined. Stamp glyph ALWAYS 'map'.

PROBLEMS SESSION 100/101 ALREADY SOLVED: /find/[id] in-app back button must set POPSTATE_MARKER_KEY before router.back(). git add -A is BANNED — stage files by name.

INFRASTRUCTURE FOR R17 (verified session 117 scoping; primitives shipped session 118): all 29 active malls have malls.latitude + malls.longitude populated; /map?mall=<slug> from R10 (session 108) accepts mall scope; BoothLockupCard (session 82) + BoothHero (session 80) + PinCallout (session 107) all augmentable without breaking changes; PWA standalone iOS Safari geolocation works.

ALTERNATIVES IF DEFERRED: (1) Tier C polish bundle (~half-session, 4 sessions deep). (2) R3 implementation. (3) Italic-Lora label face on Mapbox basemap via Studio (~half-day). (4) Promote one of 🟡 Captured rows (R8/R6/R14/R5b/R13) to design phase. (5) Investor update generation.

CARRY-FORWARDS FROM SESSIONS 78–118: **R17 Arc 1 shipped (session 118)** — primitives + smoke-test page; Arc 2 wires consumers. **R17 design-to-Ready (session 117)** — 21 decisions frozen. **Polish bundle CLEARED (session 116)**. **/login email-first redesign Shape A shipped (session 115)**. **R1 ✅ Shipped (session 114)**. **R10 ✅ Shipped (session 108)**. **R16 ✅ Shipped (session 105)**. detectUserRole canonical role primitive. pickDest matrix: admin → /, vendor → /my-shelf, shopper → /me, none → /welcome. BottomNav 6th iteration of R10 D1. <PolaroidTile> primitive owns Home masonry + /flagged + /shelf/[slug] + /post/tag + /post/preview. <EmptyState> + <FormField> + <FormButton>. /find/[id]/edit form-submit. RLS enabled on every public table. AI routes deliberately unauthed. Treehouse thesis: digital-to-physical bridge — R17 is the first feature where the digital surface MEASURES the physical bridge.

SCHEDULED AGENT: trig_017455nMVrTTZb6PxYnYcYZY fires Thu May 21 9:00 AM EDT — checks if VendorCTACard still unused → opens cleanup PR if so.
```

---

## KNOWN GAPS ⚠️ (active only)

### 🔴 Pre-beta blockers

*(Empty. T4d pre-beta QA walk re-passed session 46 end-to-end — all five exit criteria clean. Q-007 Window Sprint also remains passed per sessions 40–41. Featured banner reads restored session 48. Q-008 shopper share shipped session 50 (QA walk deferred to session 51, but non-gating — code is landed and the walk is verification not revision). No code-level regressions. Beta invites remain technically unblocked after session 50.)*

### 🟡 Remaining pre-beta polish (operational, not code-gating)

- **NEW (session 118): R17 Arc 1 shipped — primitives in isolation** ✅ — 6 runtime commits smallest→largest. lib/distance.ts haversine + lib/mapsDeepLink.ts UA-aware URL builder + useUserLocation hook (silent first-mount prompt + 30-min TTL + cross-instance sync) + <DistancePill> primitive + <LocationActions> primitive + /geolocation-test smoke page. Build clean at every commit boundary. David's iPhone QA on /geolocation-test passed clean. Arc 2 (consumer wiring across 5 surfaces) is the next-session move.
- **NEW (session 118): "Design-record-as-execution-spec — implementation runs against frozen decisions without re-scoping"** — single firing, Tech Rule candidate on second firing. 21 decisions D1–D21 from session 117 ran cleanly through Arc 1 with zero design calls or mid-session re-scoping. Cleanest realization of the session-56 design-record-as-commitment-ceremony rule since R16 Task 4 (session 102). If a future Arc 1 surfaces design re-scoping, it's a signal the design pass left axes open.
- **NEW (session 118): `feedback_testbed_first_for_ai_unknowns` is now 3-firings deep** — /postcard-test (R10 session 107) + /search-bar-test (R16 session 102) + /geolocation-test (R17 session 118) all follow the primitive-validation-in-isolation-before-consumer-wiring pattern. Tech Rule promotion-strength validated.
- **NEW (session 118): `feedback_smallest_to_largest_commit_sequencing` cumulative >99 firings** — was 93 at session 116 close; +6 this session. Already promoted-via-memory at session 88; continues to be the load-bearing discipline for shipping cadence.
- **NEW (session 118): Implementation-time decisions inside locked axes** — single firing of the pattern shape. mall_slug not mall_id in analytics payload (slug is consumer-surface canonical key); denied status cached with same 30-min TTL as granted (don't re-prompt 14 times per session); window.location.href not router.push for Navigate (router.push can't handle non-http maps:// scheme). All three are sub-decisions inside locked design axes, NOT design reversals — worth distinguishing because reversals trigger the surface-locked-design-reversals memory rule, sub-decisions don't. Tech Rule candidate on second firing.
- **NEW (session 118): /geolocation-test retirement candidate** — once Arc 2 wires consumers + David validates in production-context iPhone QA, the smoke-test page becomes dormant. Same retirement question as /search-bar-test (R16) and /postcard-test (R10) — mostly kept as live-debugging surfaces. Cleanup-not-urgent.
- **NEW (session 117): R17 Geolocation-aware discovery — design-to-Ready** ✅ — 21 decisions D1–D21 frozen + V1 + V2 mockups + design record + roadmap promotion in 1 commit `87f7a43`. 🟡 Captured → 🟢 Ready same-session (2nd R-row to graduate same-day after R16 at session 102). Implementation sequenced in 2 arcs: Arc 1 ✅ shipped session 118; Arc 2 (consumer wiring across 5 surfaces + R3 events) in 1 more session.
- **NEW (session 117): "User-clarification mid-mockup-draft surfaces hidden interpretive gap — restate before continuing"** — single firing, captured as memory file ([`feedback_user_clarification_restate_interpretation.md`](memory/feedback_user_clarification_restate_interpretation.md)). When user picks "Frame X but with element from Frame Y," restate the resulting interpretation as one explicit sentence before drafting next iteration. Caught at V2 mid-draft — David interrupted with correct interpretation. Tech Rule candidate on second firing.
- **NEW (session 117): `feedback_v2_mockup_as_fill_refinement` 3rd cumulative firing** — promotion-ready. V1 spans structure, V2 spans fill within picked structure. R10 V4-as-fill, session-115 /login V2-as-fill, R17 V2-as-fill all validate the rule.
- **NEW (session 117): `feedback_reference_first_when_concept_unclear` 5th cumulative firing** — across sessions 99/102/111/115/117. Promotion-strength validated repeatedly. R17 reference-first scan of 6 cross-domain references (AllTrails / Apple Maps / Yelp / DoorDash / Facebook Marketplace / Eventbrite) before any HTML mockup.
- **NEW (session 116): 4-item polish bundle CLEARED end-to-end** ✅ — 4 sessions deep on these carries (108→110→113→115→116). 9 smallest→largest commits cleared the bundle + 5 PostcardMallCard refinements + dormant /shelves retired. **CARRY 108→115 retires.**
- **NEW (session 116): Tier C polish bundle DEFERRED to session 118** — 3 items: (1) back button on /flagged when navigating from / (partial reversal of session-109 D1 profile-left lock; needs design conversation); (2) heart icon flicker on Home tiles (root cause: useShopperSaves async hydration); (3) Saved badge flicker on BottomNav (same root cause as 2). Items 2+3 share a single repair shape — mirror DB writes to localStorage; useLayoutEffect sync read on mount. Was recommended at session 117 open; deferred for R17 design pass.
- **NEW (session 116): PostcardMallCard fluid font-size + constant card height** — `13e9fbb`. Mall name uses 1-line clamp + measure-and-shrink useLayoutEffect (NAME_FONT_MAX 22 → MIN 16); card height becomes constant ~84px regardless of name length. Trade-off: very-long names ellipsis at 16px (uncommon in current mall set).
- **NEW (session 116): "Location" eyebrow vocabulary** — `703e21c` shipped FONT_SYS small-caps "SELECT LOCATION"; `1ba4df5` reverted post-iPhone-QA to FONT_LORA italic 11px right-aligned "Location" mirroring the same-card "from:" voice. NEW pattern: "eyebrow vocabulary should match same-card existing voice." Tech Rule candidate on second firing.
- **NEW (session 116): Cancellation ink revision per David's verbatim SVG** — `9308075` + `a46437c` (paths halved + paddingRight match) → `a730f13` reverts width back to 84 + applies David's iPhone-QA values (right:14 / opacity 0.08 / stroke v1.inkPrimary). Heavy stamp-body overlap reads as watermark texture not competing element. Surfaces `feedback_surface_locked_design_reversals` for the 20th cumulative firing.
- **NEW (session 116): Share button on /flagged + bounded-payload reasoning** — `3fd1031`. Reverses session-109 D1's "saved finds are private — null right slot." Bounded reversal: shared URL only encodes ?mall=<slug> scope, never the user's save list. Surfaces `feedback_surface_locked_design_reversals`.
- **NEW (session 116): Dormant /shelves retired** — `cf52976` (−701 lines). Outbound-callsite audit found zero runtime imports; 301 redirect at next.config.js intercepts cached bookmarks. Stale comment in `app/(tabs)/flagged/page.tsx` line 9 ("/shelves as primary booth-bookmarking surface") updated to /shelf/[slug] (correct since session 80).
- **NEW (session 116): inspect-rls.ts coverage drift** ✅ — **resolved session 116** in `04ee27d`. Added shoppers / shopper_saves / shopper_booth_bookmarks to TABLES[] with expectAnonRead:false. **Carry from session 113 retires.**
- **NEW (session 116): "User-provided verbatim values (SVG, copy, code) ship as-is — don't relitigate the spec"** — single firing. David's cancellation SVG with explicit values shipped verbatim with comment block noting iPhone-QA-derived dial. Tech Rule on second firing.
- **NEW (session 116): "Tier the ask before kicking off when scope expands mid-conversation"** — single firing. Original 4-item polish bundle expanded to 12 items mid-conversation; A/B/C tiering with C deferred kept the session a single ship rather than a half-session start. Tech Rule on second firing.
- **NEW (session 115): Login email-first redesign (Shape A) shipped end-to-end** ✅ — 4 runtime commits + 1 docs commit + 1 close commit. Reference-first scan → V1 (3 structural frames) → V2 (12 frozen decisions D1–D12) → 4-arc implementation smallest→isolated first → push → David's iPhone QA on Vercel preview passed. Net runtime change −983/+197 across `/login` (+672/−160 restructure), `/welcome` (+293 new), `/login/email` (+15/−823 redirect alias), `/vendor-request` (+13 email pre-fill). Codebase shrinks while feature improves.
- **NEW (session 115): admin-without-booth stranded-state on /login** ✅ — **resolved session 115** as side-effect of Arc 1. Action cards now query vendors row separately; admin-without-booth (David's typical config) sees only "Admin tools" card (no broken "Manage my booth"). `feedback_verify_role_conditional_doesnt_strand_non_canonical_role_holders` 2nd firing → Tech Rule promotion-ready on third.
- **NEW (session 115): D11 lexicon flag pattern** — single firing. "digital storefront" (David's first-pass copy) → "digital booth" (project canonical). Sub-pattern of `feedback_user_facing_copy_scrub_skip_db_identifiers` extending to fresh user-facing copy itself (not just historic identifiers). Tech Rule on second firing.
- **NEW (session 115): "Folding multi-route flows into one route preserves working code"** — single firing. /login/email's full ~700 LOC of OTP machinery moved verbatim into /login; /login/email retired to a 33-line redirect alias preserving query params. Net runtime change is a delete (codebase shrinks while feature improves). Tech Rule on second firing.
- **NEW (session 115): "Defer external-dep features with explicit scope-locked headroom in design"** — single firing. D3 (Apple/Google SSO) deferred but Frame A's layout has natural slot above the email card for additive SSO buttons. Future implementation doesn't require redesign. Tech Rule on second firing.
- **NEW (session 115): `/login/email` redirect alias retirement window** — D6-locked at ~30 days post-2026-06-04. Watch session 116+ for trigger date. ~5 min cleanup commit when ready.
- **NEW (session 115): `feedback_reference_first_when_concept_unclear` 4th cumulative firing** (sessions 99 / 102 / 111 / 115). Already promotion-ready since 3rd firing at session 111; this firing further validates promotion-strength.
- **CARRY (108→115): 4-item polish bundle smallest→largest** ✅ — **resolved session 116** end-to-end. All 4 items shipped: (1) `36714b0` MallSheet em-dash; (2) `cf52976` /shelves delete; (3) `3fd1031` share on /flagged; (4) `04ee27d` inspect-rls.ts shopper_* tables.
- **NEW (session 114): R1 Arc 5 RE-verification walk on `5cea49a`** ✅ — **resolved session 114**. 4/5 passed clean; Step 5 partial-pass surfaced redirect (admin-without-booth stranded; tab order middle → rightmost). Both axes shipped in `cc06ae9`. R1 graduates ✅ Shipped end-to-end.
- **NEW (session 114): BottomNav 6th iteration of R10 D1** — `cc06ae9` rightmost role-tab + Admin variant. Tab order Home → Saved → [Booth | Admin]. Saved holds stable slot 2 across role transitions. Admin precedence wins over vendor (Option A). 19th cumulative firing of `feedback_surface_locked_design_reversals` across 7+ sessions; promotion-strength validated yet again.
- **NEW (session 114): "Stable persistent tabs in nav, not role-dependent positions"** — single firing. Saved sits at slot 2 in every variant; the role-specialty slot is rightmost regardless of which role unlocks it. Generalizes beyond R10 D1 to any future role-aware nav surface (`/admin` chrome, role-aware mastheads, future R13 mall-operator nav). Tech Rule on second firing.
- **NEW (session 114): "Verify role-conditional UI doesn't strand non-canonical role holders"** — single firing. After shipping role-conditional chrome, walk through the matrix of (role × secondary-state) where secondary-state = whether the chrome's destination is meaningfully populated. Admin-without-booth was the silent-stranding case session 113 missed. Tech Rule on second firing.
- **NEW (session 113): R1 Arc 5 initial walk** ✅ — **resolved session 113** end-to-end. Phase 1 cold-start passed. Phase 2 step 9 BLOCKED by silent RLS partial-paste; resolved via HITL re-paste of policy block (1/3/3 confirmed via audit_rls_state). 3 issues surfaced + fixed in 3 commits smallest→largest: `6a17ab2` /me redirect /login → /; `2b56f40` async pickDest + `detectUserRole` helper (shopper-only users land on /); `5cea49a` role-conditional Booth tab in BottomNav (vendor + admin only).
- **NEW (session 113): Migration 020 RLS partial-paste** ✅ — **resolved session 113** via HITL re-paste of policy block into both prod + staging SQL editors. 7 policies total (1 on shoppers + 3 on shopper_saves + 3 on shopper_booth_bookmarks). Verification: `audit_rls_state()` policy_count = 1/3/3 on both projects.
- **NEW (session 113): "Migration partial-paste failure mode — RLS-on + 0-policies is the silent shape"** — single firing. After a HITL multi-statement paste that includes both `ENABLE ROW LEVEL SECURITY` + `CREATE POLICY`, verify the LAST block via `audit_rls_state()` policy_count, not just the relrowsecurity flag. Service role bypasses make INSERTs feel green even when authenticated SELECTs are silently denied. Tech Rule on second firing.
- **NEW (session 113): "Service-role INSERT + RLS-denied authenticated SELECT = post-INSERT row invisible to client"** — single firing. Corollary of the above. Any new table where /api/X uses service role + the page reads via user-token client requires explicit verification that RLS allows the read. Tech Rule on second firing.
- **NEW (session 113): "Async pickDest pattern via shared role-detection primitive"** — single firing. `detectUserRole` in lib/auth.ts becomes the canonical post-sign-in role decision layer. Reusable for future role-aware chrome (BottomNav already consumes it). Decision matrix: admin → /; vendor → /my-shelf; shopper → /; none → /my-shelf. Tech Rule on second firing.
- **NEW (session 113): "Role-conditional tab insertion via spread operator"** — single firing. `[homeTab, ...(showBoothTab ? [boothTab] : []), savedTab]` is the cleaner pattern than render-time conditional tab arrays.
- **NEW (session 113): "Standup error: misread of prior-session close block creates wrong expectation in QA walk"** — single firing. My standup said /login triage cards were "vendor / admin / shopping"; actual deployed copy is "I already have an account / I'm new to Treehouse Finds / I'm shopping." Mitigation: read app/login/page.tsx directly during standup when triage cards are part of the recommended walk, not the close-block summary.
- **NEW (session 113): 5th iteration of R10 D1 (BottomNav)** — Guest 3-tab → Vendor 4-tab → flat 4-tab → flat 3-tab → flat 2-tab → 2-or-3-tab role-conditional. `feedback_surface_locked_design_reversals` cumulative >18 firings across 6+ sessions. Future iteration captured: Booth tab for guests to manage bookmarked booths.
- **NEW (session 113): inspect-rls.ts coverage drift** ✅ — **resolved session 116** in `04ee27d`. Added shoppers / shopper_saves / shopper_booth_bookmarks to TABLES[] with expectAnonRead:false + notes referencing session 113's policy_count verification.
- **NEW (session 112): R1 Arc 4 hybrid-hook abstraction** ✅ — **resolved session 112** across 5 commits. `lib/useShopperSaves.ts` (`19ba9f2`) + `lib/useShopperBoothBookmarks.ts` (`1f70a3c`) become the SINGLE SOURCE OF TRUTH for save/bookmark state across guest + authed paths; consumer wiring across 7 files (`03ab989` + `6e45ae0` + `324d1ce`) drops 5 page-local sync loops. Cross-instance sync via custom-event broadcast; storage events for cross-tab guest sync; optimistic toggle with rollback-rebroadcast on DB error. /me reads real auth + DB counts; sign-out reverts every live instance to localStorage state in the same render cycle.
- **NEW (session 112): "Hybrid hook abstraction over local + remote backing storage with reactive cross-instance event broadcast"** — single firing. The `useShopperSaves` / `useShopperBoothBookmarks` shape is reusable for any future "guest → authed migration" feature: search-history, viewing-history, draft posts. Tech Rule candidate on second firing.
- **NEW (session 112): "Hook abstraction reduces page-level state significantly when listener fanout moves into the hook"** — single firing (5 pages stopped maintaining focus + visibilitychange sync loops because the hook owns its listener fanout). Sub-pattern of the hybrid-hook rule.
- **NEW (session 112): "Optimistic toggle with rebroadcast on rollback so sibling instances don't drift"** — single firing. The hook's rollback path doesn't just revert the toggling instance's state — it broadcasts the rollback as a fresh event, so any sibling that adopted the optimistic delta also rolls back. Tech Rule on second firing.
- **CARRY (110→112): R1 shopper accounts design pass** ✅ — **resolved session 111** in `13d5fc1` (design record + roadmap promotion + mockup) + 8 runtime commits across Arcs 1–3. Arc 4 closed at session 112; Arc 5 verification remains.
- **NEW (session 110): MallSheet trailing "—" affordance** — David's session-110 close screenshot showed empty trailing dash on each row (was previously a count). Just remove the dash entirely. ~1 line in `components/MallSheet.tsx`. Trivial follow-up.
- **NEW (session 110): Share icon on /flagged (Saved)** — currently right slot is null on Saved (per session 109 design — "saved finds are private"). David: *"Add the share icon on the saved page in the same location as the others in prep for the guest user account work."* ~5 min layout change once R1 is in flight. Add to `app/(tabs)/layout.tsx` right-slot conditional.
- **NEW (session 110): Home masonry right-column animation regression** — David noticed a quick top-down animation on the right column of thumbnails when navigating BACK to Home. Probably leftover from session 78's Track D motion or an interaction with session-109's shared layout's lifecycle. Needs reproduction (David to capture video on next walk); diagnosis after.
- **NEW (session 109+110): Route-group + shared layout pattern** — `app/(tabs)/layout.tsx` owns chrome (StickyMasthead + PostcardMallCard + BottomNav) for Home/Map/Saved. Pages render only body. Tab-to-tab nav doesn't remount layout. **Implication**: any future hook backing persisted state read in the layout needs cross-instance sync (see useSavedMallId fix). The D9 "mount-time read only" assumption from `docs/mall-filter-persistence-design.md` is INVALID — the doc needs amendment if/when referenced again; for now the `lib/useSavedMallId.ts` file-top comment is canonical.
- **NEW (session 109+110): 6 design-record reversals/extensions on R10** — D1 (twice — 109 + 110), D6, D13, D19 fully reversed (was partial in 107), D26 extended. R10 design-record cumulative reversal count is now 8 events across 3 refinement passes. `feedback_surface_locked_design_reversals` continues firing constructively; cumulative >16 across 5+ sessions.
- **NEW (session 110): "Stale state in long-lived shared layout when child pages mutate persisted state"** — single firing. Tech Rule on second.
- **NEW (session 110): "Primitive interactivity gated on prop presence (button vs div)"** — single firing (PostcardMallCard renders `<div>` when no `onTap` provided). Tech Rule on second.
- **NEW (session 110): Auth chrome relocation pattern is now 3 migrations deep** (≤87 right → 90 BottomNav-tab → 109 masthead-left). Each migration was a deliberate response to product evolution — the reversal-surface memory gave clean traceability. Single firing this session.
- **R10 Arc 3 — Mapbox custom-styled map integration** ✅ — **resolved session 108** in 5 commits (`5959282`, `21b08de`, `e643507`, `4acf4f3` + diagnostic `9fb88b0` + dial `24cbae0`). `<TreehouseMap>` component shipped with cartographic warm-cream palette via runtime layer overrides on `light-v11` + leaf-bubble pins + scope-driven flyTo/fitBounds + peek-then-commit interaction with PinCallout. Italic-Lora label face deferred until Mapbox Studio.
- **R10 Arc 4 — `/shelves` disposition cleanup** ✅ — **resolved session 108** in `ef2d2a6`. Outbound link audit found ZERO runtime callsites; 301 redirect to `/map` via next.config.js. Page file kept dormant in repo.
- **Mapbox URL-restriction tightening** ✅ — **resolved session 108** (carry retires). David tested on PWA both walks; production-PWA URL is authorized. Session-107 `*.vercel.app` rejection didn't block the URL David's PWA is bound to.
- **NEW (session 108): Italic-Lora label face on cartographic basemap remains deferred** — requires Mapbox Studio; lands when David moves to a Studio-hosted style URL (~half-day). Until then labels render in `v1.basemap.label` ink + paper-cream halo using Mapbox's default sans face.
- **NEW (session 108): `app/shelves/page.tsx` dormant in repo** — redirect intercepts before route resolution, but the file still ships in the build (~10kb gzipped). Optional cleanup commit.
- **NEW (session 108): "PWA service-worker cache lag on dependency-graph change"** — single firing this session (first-walk empty cream = stale bundle from before mapbox-gl entered the dependency graph). Tech Rule on second firing.
- **NEW (session 108): "Three structural fixes in one diagnostic when symptom is ambiguous"** — single firing this session as ambiguous-symptom extension of `feedback_kill_bug_class_after_3_patches`. Tech Rule on second firing; if it fires again, candidate for memory promotion.
- **NEW (session 108): "Container height percent-on-flex-item ambiguity → use absolute positioning"** — single firing.
- **NEW (session 108): "Dynamic fit-to-data over static bounding box"** — single firing as a generalizing principle (auto-tightens with data clusters; auto-expands with data spread).
- **NEW (session 107): "Same primitive, divergent tap behavior across surfaces"** — single firing session 107 (PostcardMallCard ships identically across Home / Saved / Map but onTap differs). Tech Rule on second firing of "centralize-the-change-UI-on-one-surface" pattern.
- **NEW (session 107): "Multi-design-record-reversal-burst with explicit traceability"** — single firing session 107 (5 reversals across R10 + R16). Tech Rule on second firing.
- **NEW (session 107): NEW MEMORY captured** — `feedback_lora_lineheight_minimum_for_clamp.md`. 3rd firing of the rule (sessions 82+83+107). Generalizes session-83 scope (≤14px) to ANY clamped Lora regardless of font size.
- **CARRY (session 105→110): Run staging backfill** — David hit `Missing ANTHROPIC_API_KEY in .env.staging.local`. Optional + non-blocking. **Aging carry — 5 sessions.**
- **CARRY (session 105→110): "Mirror sanitizer fix to all AI-call consumers in adjacent commits"** — single firing session 105. Tech Rule on second firing.
- **CARRY (session 105→110): "Custom-rendered primitive as kill-the-bug-class fix for iOS WebKit caret quirks"** — single firing. Pattern is documented in components/SearchBar.tsx file-top "Custom caret" comment block.
- **CARRY (session 105→110): "Reverse-locked-decision" memory now at >16 firings cumulative across 5+ sessions** — promotion-ready many times over; rule fires constructively every time.
- **CARRY (session 104→110): iPhone QA on `/vendor-request` success-state masthead change** — back/share dropped, wordmark stays. Still un-walked. ~3 min throwaway-email submission. **Aging carry — 6 sessions.**
- **CARRY (session 104→105): Apple splash screen `apple-splash-screen-1024x1024.png` not regenerated** — acceptable since splash is transient; a fresh 1024+ source from David is the right input.
- **CARRY (session 104→105): Watch for second firing of "terminal-success vs mid-flow masthead distinction"** on `/setup` success or `/post/preview` post-publish.
- **CARRY (session 103→105): Activate Copper Awning Flea Market via /admin Locations tab** (~30 sec HITL). Status defaults to `draft` from session-103 paste; flip to `active` (or `coming_soon`) when ready to surface in shopper pickers + Home feed.
- **NEW (session 103): Staging missing migrations 009 + 010 + 011** — re-surfaced when first SQL paste failed silently against staging (column `malls.status` doesn't exist). Known carry from session 92, no longer hypothetical. ~5 min HITL paste of all three migrations into staging dashboard. Worth doing before R16 implementation lands so staging stays a useful preview surface for Task 1 (migration 019).
- **NEW (session 103): scripts/add-mall.ts is now the standard tool for new mall locations.** Future additions: `npx tsx scripts/add-mall.ts "<google-maps-url>" --phone=... --website=... --apply`. Consider adding `npm run add-mall` shortcut in package.json on 2nd firing.
- **NEW (session 103): "Script-first over SQL-dump-first" rule** — first positive firing since session 57 promotion. Validating-strength: future mall additions are now scripted, not pasted. Watch for next firing on any other DB-side recurring operation.
- **CARRY (session 101): iPhone QA on /flagged final state** — session-99 ship still un-walked on real device. Cold-start across multi-booth + All-Kentucky-Locations scope + filterHidesAllSaves empty state + editorial preamble plurality + "Explore the booth →" CTA tap fires `flagged_booth_explored` R3 event. ~15-20 min.
- **NEW (session 101): Chrome browser-native back chevron timing race** — top-of-Chrome's UI back button (not the in-app back) still has the popstate-after-[id]-effect race because we have no JS hook to fire before browser-native back. Bounded harm: only Chrome browser, not Safari, not PWA. PWA fully covered. If beta feedback surfaces it, separate fix shape needed (popstate-driven recovery on /find/[id] mount).
- **NEW (session 101): vendor-posts cache invalidation coverage gap** — currently invalidates on PATCH success at /find/[id]/edit + /post/edit/[id]. NOT invalidated on: post delete (deletePost), new post add (post-publish flow), admin actions on /shelves. If a user flow hits the carousel within a session after one of these state changes, the carousel shows a stale list. ~10 min audit + extension.
- **NEW (session 101): debug toast harness pattern is now project-canonical** — `feedback_visibility_tools_first.md` validated 4× across recent sessions. Worth building reusable `<DebugToast>` primitive in `components/` that any page can import + push to. Single firing as a primitive; build on second firing.
- **NEW (session 101): docs/product-state.md inadvertently swept onto main** via `git add -A` violation in commit `3a7af1a`. David accepted; session-99 carry "uncommitted/mid-edit" closed in confusing way. If David wants the narrative to stay in flux he can keep editing on top.
- **CARRY (session 100→101): wasRecentPopstate(800ms) window is conservatively wide** — false-positive only fires if user clicks back AND a Link within 800ms; bounded harm because pendingScrollY=null gates it. Worth iPhone QA stress test on slow scroll-then-back-then-forward.
- **CARRY (session 100→101): The diagnostic console.log/toast harness pattern is reusable** — pattern fired session 100 (console.log) + session 101 (toast). Documenting as a template now justifiable.
- **CARRY (session 100→101): getPostsByIds + getVendorPosts SELECT extensions** — slight payload bump (~30-100 bytes/post). If feed pagination ever fires (R5b), reconsider.
- **CARRY (session 100→101): Phase D candidate** — extend swipe-context handoff to /post/preview if vendor-self peer-nav becomes desirable. Currently /my-shelf and other vendor surfaces deliberately omit. Beta watch-item.
- **CARRY (session 99→101): Booth-bookmark glyph removed from /flagged** — beta watch-item.
- **CARRY (session 99→101): Per-find unsave only via /find/[id]** — watch for sync gaps.
- **CARRY (session 99→101): "Form-submit replaces autosave"** — 2nd firing. Tech Rule promotion-ready.
- **CARRY (session 98→99): iPhone QA pass 4** — verify form-submit conversion on /find/[id]/edit (Status pills don't save instantly + caption empty-string nullifies + title validation), side-by-side check + dotted layout on /post/tag (1.2s dwell + check overlay weight + dotted placeholder visual weight), pencil bottom-right placement on /find/[id], + flow-continuity goal across /post/tag + /post/preview + /find/[id]/edit (do titles land at the same vertical position now?).
- **NEW (session 98): Verb mismatch on /find/[id]/edit** — subtitle "Click submit when finished" + button "Post changes." Shipped per literal directive. If reads off, harmonize either direction (button → "Submit" OR subtitle → "Tap Post changes when finished").
- **NEW (session 98): Status toggle behavior shift** — pills no longer save instantly; vendor must tap Post changes. The "Marked sold" confirmation banner is gone (was tied to immediate-write). Watch for vendor confusion in beta feedback.
- **NEW (session 98): Caption empty-string nullifies existing caption** — typing then deleting caption + submitting writes null to caption column. Acceptable but worth verifying.
- **NEW (session 98): /find/[id]/edit on iPhone SE (375×667)** — with caption auto-grow on long captions, page might still scroll despite headroom tightening. Rule is "aim for one screen" not "force fit."
- **NEW (session 98): Check overlay tuning** — 32×32 frosted-paper bubble + green Check 16px on /post/tag find polaroid. Easy dial-back to 28×28 with 14px Check if too prominent.
- **NEW (session 98): Dotted placeholder visual weight** — 2px dashed minimal style. Easy swap to "Option B" with paperWarm frame if too sparse on real device.
- **CARRY (session 97→98): /vendor-request, /post/edit/[id], /admin/login still use StickyMasthead** — session 98 extended the vendor/admin-internal-flow rule to /post/tag; these 3 are next candidates but require David's explicit ask.
- **NEW (session 98): "Form-submit replaces autosave when batch save provides clearer mental model"** — single firing as a model-choice. Tech Rule on second firing.
- **NEW (session 98): "Side-by-side progression layout — same outer structure across flow steps with placeholder slots that fill"** — single firing.
- **NEW (session 97): "Per-context sizing on a swept primitive"** — 3rd firing across sessions 82 + 96 + 97. Tech Rule promotion-ready.
- **NEW (session 97): "User-references-existing-page as the spec, not a fresh mockup"** — first firing. Tech Rule on second firing.
- **NEW (session 97): "Ship a reasonable midpoint when math gives an unbounded answer; accept dial-back"** — first firing.
- **R3 admin-Events stale-data mystery** ✅ — **resolved session 73**. Root cause: Next.js HTTP-level data cache intercepting `@supabase/supabase-js`'s internal `fetch()` calls. Two-line fix in [`lib/adminAuth.ts`](lib/adminAuth.ts) (`global.fetch` wrapper). Diag strip + raw probe stay as durable visibility tooling per David's session-73 decision until admin tab retires post-Q-014. Verbose console.logs in `/api/admin/events/route.ts` are now duplicative; cleanup deferred.
- **`/find/[id]` `navigator.share()` instrumentation gap** ✅ — **resolved session 73**. `find_shared` event with `share_method` payload, intent-capture semantic.
- **Items 1 + 5 + 6 + 7 (Gemba polish quartet)** ✅ — **resolved session 74** in `30b9922`. Alphanumeric booth keyboard, FB CTA retired, AddFindTile to grid tail, my-shelf 9-cell placeholder grid dropped.
- **Item 4 (post-publish vendor-param preservation)** ✅ — **resolved session 74** in `d52b149`. Three router.push call sites in /post/preview preserve `?vendor=<id>` so admin lands on booth-of-origin.
- **Items 2 + 3 (booth name + mall edit)** ✅ — **resolved session 74** in `c5437df`. EditBoothSheet + PATCH `/api/admin/vendors` + Pencil bubble rewire on `/shelves`.
- **"Add a booth" affordance restored on /shelves** ✅ — **resolved session 74** in `c5437df`. AddBoothTile + AddBoothSheet (third oscillation, intentional this pass per [`docs/booth-management-design.md`](docs/booth-management-design.md)).
- **Item 1 (session 75) — admin login redirect** ✅ — **resolved session 75** in `60a7a11`. `pickDest(user)` helper routes admins to `/` by default; explicit `?redirect=/foo` still wins.
- **Item 6 (session 75) — BoothHero lightbox on tap** ✅ — **resolved session 75** in `eef1746`. Transparent overlay button at z-5 sandwiched between photo container and pencil/post-it. Mounts only when heroImageUrl present.
- **Item 7 (session 75) — booth-numeral font system** ✅ — **resolved session 75** across `d747b71` + `01a6b44`. `FONT_NUMERAL` (Times New Roman) replaces `FONT_IM_FELL` on Variant B booth lockup numerals + MallScopeHeader count chips. Project-wide rule frozen.
- **Items 2 + 3 + 4 + 5 (session 75) — /vendor-request redesign** ✅ — **resolved session 75** across `a427ee7` + `fbbba21`. One-screen layout, owner-ack checkbox, hard-required Mall+Booth#, FONT_SYS photo dropzone copy, intro paragraph dropped.
- **Item 1 (session 76) — masthead disappears on back-nav** ✅ — **resolved session 76** in `b5470da`. Loading + !post branches now render `StickyMasthead` with back button.
- **Items 3 + 4 (session 76) — green tokens** ✅ — **resolved session 76** in `d0c4ce3` + `fd1a874`. Treehouse Finds wordmark + "Booth" eyebrow + booth numeral all `v1.green` across every applicable surface.
- **Item 2 (session 76) — find-title centered + price below** ✅ — **resolved session 76** across `a2cf548` (record) + `fd1a874` (impl bundle). Frame B picked. Em-dash retired.
- **Item 5 (session 76, E1–E5+E7) — animation consistency** ✅ — **resolved session 76** across `ecb8ef8` + `3832fc1`. 5 motion tokens in [`lib/tokens.ts`](lib/tokens.ts); Booths VendorCard gains tap feedback; empty states unified across 4 primary tabs.
- **Migration 013_owner_acknowledged.sql** ✅ — **resolved session 77** (David ran HITL paste this session per session-77 beat 1).
- **Item 1 (session 76 attempt + session 77 4 attempts) — masthead disappears on PWA back-nav** ✅ — **resolved session 77** in `166ed59` after 5 attempts. Final fix: `position: fixed` + spacer (kills sticky-paint bug class). Earlier 4 attempts (`b5470da`, `361e1de`, `c968c1a`, `d429bbb`) addressed wrong layers — preserved in history as audit trail.
- **Track D phases 1–4** ✅ — **shipped session 77** across `c968c1a` (mockup bundled), `d3ddffb` (design record), `e601db5` (testbed). D1–D14 frozen. Three thumbnail surfaces in scope. **NEW: position:fixed + spacer is masthead's underlying primitive** — `MASTHEAD_HEIGHT` const in [`components/StickyMasthead.tsx`](components/StickyMasthead.tsx) is single source of truth. **NEW: `/transition-test` is durable diagnostic infrastructure** — keep live for layoutId regressions.
- **Track D phase 5 — surface rollout** ✅ — **shipped session 78** across 11 runtime commits and 6 iPhone-QA cycles. Three surfaces originally live: feed → /find/[id], /shelves → /shelf/[slug], /flagged → /find/[id]. **Session 80 retired the /shelves source side** when the photo went away from /shelves rows; that surface is now a graceful-no-op morph rather than a shipped morph (destination layoutId on /shelf/[slug] BoothHero preserved; framer-motion gracefully no-ops when source is absent). Two surfaces still live: feed → /find/[id], /flagged → /find/[id]. Critical fix `c3b9541` introduced the **preview cache pattern** (sessionStorage `treehouse_find_preview:${id}` + module-scope `cachedFeedPosts`) that any future shared-element work will need to reuse. **NEW carry-forward: D11 + D12 design record values overridden by on-device feedback** — all entrance delays compressed to 0; post-it crossfade replaced with zoom-in 1.15→1; bubble timing matches photograph morph. Design record stays as historical reference; runtime values only in CLAUDE.md session 78 block. Operational backlog: amend the design record with a "Runtime Overrides" section + a "Source side retired on /shelves session 80" addendum.
- **Item 5 (session 80) — wordmark TNR upright ink-black 18px** ✅ — **resolved session 80** in `2efced5`. `StickyMasthead.tsx` `WORDMARK_DEFAULT` rewritten: `FONT_NUMERAL` (Times New Roman) + `fontSize: 18` + `color: v1.inkPrimary`, dropped italic + the -1px translateY lift. Visible on every screen that uses StickyMasthead.
- **Item 4 (session 80) — bookmark relocated masthead → BoothHero photo corner** ✅ — **resolved session 80** in `786de81`. Design record at [`docs/bookmark-relocation-design.md`](docs/bookmark-relocation-design.md) D1–D7. `BookmarkBoothBubble` gained `"hero"` size variant (36×36 frosted, 18px glyph). `BoothHero` gained optional `saved` + `onToggleBookmark` props; bubble renders as SIBLING of photograph motion.div at top:8 right:8, NO own layoutId. `/shelf/[slug]` masthead right slot collapses to share-airplane-only. **NEW carry-forward: needs iPhone QA against banner-aspect hero photo** (item 4 sized to match /find/[id] flag's 4:5 photo).
- **Item 2 (session 80) — Booths page Option C2 row pattern** ✅ — **resolved session 80** in `895129b`. Design record at [`docs/booths-row-pattern-design.md`](docs/booths-row-pattern-design.md) D1–D9. `VendorCard` rewritten as full-width row, photo retired, no bio, bookmark on far-left, lockup mirrors /flagged. **D8 admin chrome (Pencil + Trash inline before booth stack) was a single-firing implementation-time decision NOT in the V2 mockup** — needs admin walk-through to confirm. **NEW carry-forward: row density on real content needs iPhone QA.** Old grid was ~4 booths/scroll; row pattern is ~7-8/scroll. Long vendor names + alphanumeric booth IDs will stress-test ellipsis behavior.
- **Item 1 + Item 3 (session 80) — nav rename + curated text** ✅ — **resolved session 80** in `2e40732` + `cdac99d`. Trivial copy changes.
- **Two polish items (session 80)** ✅ — **resolved session 80** in `eb6f942`. /shelves MallScopeHeader gains the same fade-up entrance as Home + /flagged; /find/[id] cartographic mall subtitle marginTop 4 → 2.
- **Item 1 (session 81) — nav stroke 2.0** ✅ — **resolved session 81** in `333ea3b`. BottomNav 5 icons strokeWidth 1.7 → 2.0.
- **Item 3 (session 81) — photo-overlay bubble bg lighter** ✅ — **resolved session 81** in `79306a0`. `rgba(232,221,199,0.78)` → `rgba(245,242,235,0.85)` across 5 surfaces (BookmarkBoothBubble tile + hero, masonry tile flag, /flagged tile flag, /find/[id] frostedBg + bottom flag/edit). Session 80 BoothHero bookmark iPhone QA carry-forward is now resolved through this lighter bubble shipped at the same time.
- **Item 2 (session 81) — booth lockup A3 dashed-fill-lift card** ✅ — **resolved session 81** in `ae4593c` + `846b673` (BoothHero post-it eyebrow follow-on). A3 wrapper across `/shelves` rows + `/flagged` section headers: dashed green border + paper-cream 0.55 fill + soft drop shadow + numeral 26 → 22. BoothHero + `/find/[id]` post-it eyebrows swapped to small-caps FONT_SYS to match (commits `846b673` + `bd0011b`). Mockup files at [`docs/mockups/demo-prep-refinement-v1.html`](docs/mockups/demo-prep-refinement-v1.html) + [`docs/mockups/demo-prep-refinement-v2.html`](docs/mockups/demo-prep-refinement-v2.html) serve as the design record (no separate `.md` since this was tactical visual polish, not a new structural pattern).
- **Tweak 1 (session 81) — /flagged vendor/mall gap** ✅ — **resolved session 81** in `a0e2862`. marginTop 6 → 2 to match /find/[id] cartographic block.
- **Tweak 2 (session 81) — post-it warmer + cartographic card matched** ✅ — **resolved session 81** in `c6e5273` + `154145f`. v1.postit `#fffaea → #fbf3df`. Cartographic "Find this item at" card on /find/[id] inline `background: v1.postit` (not global `v1.inkWash` swap, which would have over-broadened to 18+ surfaces).
- **Tweak 3 (session 81) — /find/[id] post-it eyebrow** ✅ — **resolved session 81** in `bd0011b`. Italic IM Fell 14px → small-caps FONT_SYS 9px / 0.12em / weight 700 / uppercase to match BoothHero post-it.
- **Tweaks 4 + 5 (session 81) — "Mall" → "Location" copy scrub + "All Kentucky Locations" picker** ✅ — **resolved session 81** in `0fab4a0`. 13 files of user-facing strings updated; internal identifiers (`Mall` types, `malls` table, `mall_id` props, `mall_activated`/`mall_deactivated` event keys, `/api/admin/malls` routes, component names) deliberately preserved. **NEW carry-forward: "All Kentucky Locations" title fit risk on narrow phones** (26px IM Fell + chevron + side padding ~290–330px on a 390px-wide phone). If wraps awkwardly, options: drop to 22px, abbreviate "All Locations", or stack on two lines. Easy to revise.
- **Session 80 carry: Item 4 BoothHero bookmark needs iPhone QA against banner photo** ✅ — **resolved session 81 implicitly**. The session 81 bubble bg lightening (paper-warm 0.85) was shipped + David QA'd via Vercel preview without flagging the BoothHero corner bubble against banner-aspect photos — assumed acceptable.
- **Session 80 carry: Item 2 row density iPhone QA** — **partially addressed session 81**. A3 lockup wrapper added to rows; David QA'd the A3 card without flagging row-density issues. Real-content seeding still the load-bearing test.
- **Session 80 carry: Item 5 wordmark 18px against masthead bubbles** — **session 81 untouched.** No regressions reported during iPhone QA cycle. Likely acceptable.
- **Session 80 carry: Item 2 D8 admin chrome** — **session 81 untouched.** No admin walk-through this session; still a single-firing implementation-time decision worth admin verification.
- **NEW (session 81): "All Kentucky Locations" title fit on narrow phones** — see tweaks 4+5 entry above.
- **NEW (session 81): A3 lockup numeral 22 vs old 26 — visual rhythm on real content** — card-padding consumes the 4px lost. Real-content seeding will stress-test against varied alphanumeric booth IDs.
- **NEW (session 81): Post-it color #fbf3df subtle vs prior #fffaea** — RGB delta (-4, -7, -11). If still reads white in real-content QA, easy refinement to push warmer (e.g. `#f9eed5`).
- **NEW (session 81): Cartographic card bg matched via inline `v1.postit` not via global token swap** — single firing. If we accumulate more "this element is in the cartographic-card family" surfaces, extract a `v1.cartographicBg` semantic token. Not needed yet.
- **NEW (session 81): Q-005 admin claimed-booth deletion gap surfaced again** — beat 10 admin SQL HITL for Kentucky Treehouse + Ella's Finds test booths. Trash on `/shelves` is gated to orphan booths only (session 74 D8). Cleanup tool would resolve. Sprint 6+ candidate.
- **NEW (session 81): "User-facing copy scrub: skip DB/API/event/type identifiers"** — first firing. Tech Rule on second.
- **NEW (session 81): "Inline bg vs token swap when token affects too many surfaces"** — first firing.
- **NEW (session 81): "V2 mockup as fill-refinement within a picked option"** — first firing pattern shape; useful for "I picked option A, but with this concern" iterations.
- **NEW (session 81): "Mid-session iPhone QA on Vercel preview shortens redirect cycle"** — first firing as a structural pattern (vs ship-then-QA-next-session). Compresses 2-session arc into one. **2nd firing session 82** — promotion-ready.
- **Item 1 (session 82) — booth # optional** ✅ — **resolved session 82** in `2af064d`. Client-side red-asterisk → `(optional)` badge + drop client validation + drop server-side `!booth_number?.trim()` validation. Server dedup query already handled null booth.
- **Item 2 (session 82) — Mall → Location on /vendor-request** ✅ — **resolved session 82** in `e924664`. Single label swap + validation error string. The form was missed in session 81's 13-file scrub.
- **Item 3 (session 82) — /vendor-request typography pass + system-wide IM Fell retirement + Option C label primitive across 5 form surfaces** ✅ — **resolved session 82** across 4 commits (`08bf5c4` Lora infra + `b444c5e` project-wide font sweep + `6d26df6` /vendor-request page changes + `e446c11` form-label primitive sweep). New title "Set up your digital booth" + permission/value intro paragraph. **IM Fell completely retired from the codebase** — Lora is the project-wide literary serif via `next/font/google` + `FONT_LORA` token. Option C label primitive (Lora upright, 15px /vendor-request + 13px sheets, ink-mid) swept across BoothFormFields + AddBoothInline + admin/login + post/preview Field. Photo dropzone + ack-card unified to Lora (was FONT_SYS). Mockup files: [`docs/mockups/vendor-request-typography-v1.html`](docs/mockups/vendor-request-typography-v1.html) + [`docs/mockups/vendor-request-typography-v2.html`](docs/mockups/vendor-request-typography-v2.html).
- **/shelves bookmark filter count bug (session 82)** ✅ — **resolved session 82** in `dfed189`. Chip rendered `bookmarkedIds.size` (global localStorage set) instead of `bookmarkedInScopeCount` (already computed at line 523). One-line fix.
- **/shelves rows without booth # are shorter + text cut off (session 82)** ✅ — **resolved session 82** in `f187165`. New BoothLockupCard primitive uses `min-height: 60` to keep rows uniform regardless of booth-number presence. Empty-state hides BOOTH eyebrow entirely (per Q1 = c); name claims full row width.
- **BoothLockupCard shared primitive extraction + applied to /shelves + /flagged (session 82)** ✅ — **shipped session 82** in [`components/BoothLockupCard.tsx`](components/BoothLockupCard.tsx) + `f187165` (apply to /shelves) + `bf8ac24` (apply to /flagged + booth bookmark wiring). Visual: v1.postit bg, 1px hairline, 10px radius, 12/14 padding, no shadow. /find/[id] cartographic card stays inline (intentional per David — *"no change to this component card"*) but visually identical. **Session 81's A3 dashed-fill-lift card is RETIRED** in favor of this shared primitive.
- **/flagged section header booth bookmark added (session 82)** ✅ — **resolved session 82** in `bf8ac24`. BoothGroup type gained `vendorId` field. State + handler mirror /shelves (boothBookmarkKey + R3 v1.1 booth_bookmarked / booth_unbookmarked events). Synced on mount + focus.
- **Descender clip on Lora vendor names (session 82)** ✅ — **resolved session 82** in `a045058`. lineHeight 1.25 → 1.4 across BoothLockupCard primitive AND /find/[id] inline (had the same bug, just hadn't been caught in QA because David's seed data lacked descender-heavy vendor names).
- **NEW (session 82): IM Fell inline comments are documentation rot.** ~20 references across 15 files ("italic IM Fell vocabulary," "matches IM Fell italic," etc.). Functional sweep was clean; comments lag. Opportunistic cleanup over future sessions.
- **NEW (session 82): Form-label sweep skipped non-`<label>`-tag form chrome** on AddFindSheet, post/preview body, setup page. Section headers in italic Lora may want similar Option C treatment if they fire as recurring readability concerns. Defer.
- **NEW (session 82): BoothLockupCard primitive could extend to /shelf/[slug] BoothHero** if David ever wants the post-it to match the card primitive. Currently intentionally NOT matched.
- **NEW (session 82): "Bug surfaced in extracted primitive AND inline copy of same primitive — fix both in same commit"** — first firing (descender clip in BoothLockupCard + /find/[id] inline simultaneously). Single firing.
- **NEW (session 82): "Per-context sizing on a swept primitive"** — first firing (Option C label at 15px on full pages, 13px on sheets — label sized relative to its input). Single firing.
- **NEW (session 82): "User-facing copy scrub: skip DB/API/event/type identifiers"** — **2nd firing.** Promotion-ready.
- **NEW (session 82): "V2 mockup as fill-refinement within a picked option"** — **2nd firing** (V1 = 3 label primitives within IM Fell / V2 = 3 fonts within picked Option C). Promotion-ready on a 3rd firing.
- **NEW (session 82): "We continue to run into this issue" / "Every time I see X" signals system-level concern** — first firing as a memory ([`feedback_recurring_phrase_signals_system.md`](/Users/davidbutler/.claude/projects/-Users-davidbutler-Projects-treehouse-treasure-search/memory/feedback_recurring_phrase_signals_system.md)). Pattern fired 2× in session 82 (typography + booth lockup). Memory captures the rule.
- **Postcard PoC reverted (session 83)** ✅ — explored postcard/paper visual language as session opener. V1 mockup (3 additive frames: texture / +frame / +stamp) → David picked Frame A → 3 implementation iterations on Home — David's verdict after iPhone QA: *"the background is subtly distracting as it almost feels like there is dust on the screen due to the texture."* Full revert (`c3d5528`) returned to session 82 close baseline. Mockup retained as exploration record. paper.png in /public retained (David-uploaded; cleanup candidate).
- **Polaroid evolved tile pattern (session 83)** ✅ — **shipped end-to-end across 4 surfaces.** Home MasonryTile (`55d9c77` + `2df6a78`) + /flagged FindTile + /shelf/[slug] WindowTile + ShelfTile (`6152511`). Warm cream paper `#faf2e0`, 4px radius, dimensional dual-shadow (`0 6px 14px 0.20` far + `0 1.5px 3px 0.10` close), 7px photo mat top/sides. Three near-identical wrappers — did NOT extract shared `<FindTile>` primitive (premature abstraction). If a fourth surface adopts polaroid OR all need to evolve in lockstep, refactor candidate.
- **Tile title+price centering treatment (session 83)** ✅ — `b8c8e67` David iPhone QA redirect. Title+price center-aligned horizontally, vertically grouped via flex column + justify-center + items-center within metadata block. Price `FONT_SYS 12 → FONT_LORA 14` matching title size — same-size-different-color pattern lifted from /find/[id] hero (Lora 32px both, only color differs between `inkPrimary` and `priceInk`). marginTop 3 → 4 for tighter "locked together" rhythm. Applied to FindTile + WindowTile + ShelfTile.
- **Tile metadata block height-locking (session 83)** ✅ — `e19e9fe` David iPhone QA redirect on /flagged. `minHeight: 76` → `height: 76` (locked) + bottom padding 11 → 4 to fit 2-line title + price worst case exactly (39.2 + 4 + 19.6 + 13 padding = 76). Heterogeneous title content (1-line vs 2-line) no longer breaks row consistency. Applied to FindTile + WindowTile + ShelfTile.
- **/find/[id] strip centering + "shelf" → "booth" rename (session 83)** ✅ — `b804ee6`. Centering format applied to ShelfCard metadata block (textAlign center, lineHeight 1.4, width 100%, flex column + justify-center + items-center, height locked at 56, bottom padding 11→4). **Did NOT** apply polaroid wrapper styling — kept inkWash + hairline + 6px radius + subtle shadow per "polaroid skips navigate/detail surfaces" rule. Section eyebrow rendered text "More from this shelf…" → "More from this booth…".
- **Tile title descender clearance (session 83)** ✅ — title lineHeight 1.2 → 1.4 across FindTile + WindowTile + ShelfTile + ShelfCard. **2nd firing of session 82's vendor-name fix** — Lora descenders extend ~5px below baseline; at 14px text + 1.2 lineHeight + overflow:hidden + WebkitLineClamp:2, descenders ('g', 'y', 'p') clipped on line 2.
- **NEW (session 83): paper.png in /public** — 2.7MB unused asset David uploaded for postcard PoC. Cleanup candidate; left in place per "don't presume to delete user-uploaded assets" instinct.
- **NEW (session 83): 3 inline "More from this shelf" code comments** in [`app/find/[id]/page.tsx`](app/find/[id]/page.tsx) (lines 14, 31, 1224) — documentation rot. Same pattern as session 82's IM Fell comments. Opportunistic cleanup.
- **NEW (session 83): Three near-identical polaroid wrappers across FindTile + WindowTile + ShelfTile + ShelfCard** — shared `<FindTile>` primitive extraction trigger if a fourth surface adopts polaroid OR all need to evolve in lockstep. Don't extract until that trigger fires.
- **NEW (session 83): "Browse vs navigate/detail rule for material chrome"** — first firing as a system rule. Polaroid (heavy material chrome) belongs on browse/collect surfaces (Home, /flagged, /shelf/[slug] grid). Light card chrome stays on navigate/detail surfaces (/find/[id] strip). BoothPage post-it hero counts as "anchor" not "tile" — no polaroid there. Tech Rule candidate on second firing.
- **NEW (session 83): "Match a primary-page typographic pattern when extending visual identity to tiles"** — first firing. Lifted /find/[id]'s hero title+price treatment (Lora 32px both, same weight, marginTop 2, only color differs) to 14px tiles (same family, same weight, marginTop 4, only color differs). Brand-anchoring at scale. Tech Rule candidate.
- **NEW (session 83): "Lock fixed-height + reduce padding to fit worst case for row consistency"** — first firing for heterogeneous-content tiles (1-line vs 2-line titles). Tech Rule candidate.
- **NEW (session 83): "Lora needs lineHeight 1.4 minimum when overflow:hidden + ≤14px text + line-clamp"** — **2nd firing** (session 82 vendor names + session 83 tile titles). Promotion-ready as concrete typography rule.
- **NEW (session 83): "Revert to clean baseline before pivoting directions"** — captured as new memory ([`feedback_revert_to_clean_baseline_before_pivot.md`](/Users/davidbutler/.claude/projects/-Users-davidbutler-Projects-treehouse-treasure-search/memory/feedback_revert_to_clean_baseline_before_pivot.md)). Don't carry abandoned changes into a pivot.
- **Phase 3 scroll-restore on /flagged + /shelf/[slug] (guest path)** ✅ — **resolved session 85** in commits `6339f0f` + `c723792`.
- **Admin back button on /my-shelf?vendor=<id>** ✅ — **resolved session 85** in `5e64b7a`.
- **/my-shelf admin scroll-restore** ✅ — **resolved session 86** in `0c53fb3` (refuse-to-write-0 fix) + `e311dce` (cleanup). Bug: Next.js App Router scroll-to-top fired real scroll events on outbound navigation; the scroll listener wrote 0 to storage, clobbering the user's good scroll position. Fix: write-side filter (`if (rounded <= 0) return`). Confirmed via iPhone QA. **Phase 3 scroll-restore now closed end-to-end across all 3 surfaces.** Session 85's structural patches (BFCache pageshow handler, popstate, useSearchParams reactivity, dual-write storage, retry-scrollTo loop) stay in place as harmless defensive code; aggressive revert is a follow-up under extended QA.
- **Safari Web Inspector via USB cable** ✅ — **resolved session 86**. Setup procedure validated end-to-end; David has it operational; first iOS-Safari bug closed using this path. Memory file [`reference_ios_safari_web_inspector.md`](/Users/davidbutler/.claude/projects/-Users-davidbutler-Projects-treehouse-treasure-search/memory/reference_ios_safari_web_inspector.md) captures setup + outcome + 2 setup-time snags hit (admin auth state per-device, iPhone-not-appearing-in-Develop-menu) as carry-forward troubleshooting.
- 🟡 **CARRY (session 85→86): /find/[id] peer-nav flag glyph + horizontal carousel scroll loss** — out-of-scope from Phase 3. Navigating /find/[id-A] → "More from this booth" → /find/[id-B] → back loses (a) the flag glyph on /find/[id-A] and (b) horizontal scroll position in the More-from-this-booth row. Probably bookmark-state hydration race + DOM-level horizontal scroll preservation. ~30 min standalone session. Now Inspector-debuggable the same way /my-shelf was.
- **NEW (session 86): "Hypothesis + diagnostic + fix in same commit cycle when confidence ≥80%"** — first firing as a structural pattern. Inspector showed bursts of 5-8 zero-writes per cycle → 90% confident on Next.js scroll-to-top → shipped diagnostic AND fix in `0c53fb3` rather than waiting for another verify-then-fix round. Compresses 2 cycles into 1. Tech Rule candidate on second firing.
- **NEW (session 86): "Refuse-to-write-0 pattern (write-side filter on meaningless restore values)"** — first firing as a project-specific scroll-restore primitive. If any future scroll/persistence primitive needs the same defense, the canonical fix shape is: filter at the write site, not the read site. 0 is a meaningless restore target since empty storage already restores to 0 by default. Captured in code comments.
- **VALIDATION (session 86): "Cap speculative patching at 3 rounds + escalate to device-level visibility"** — 2nd firing with FULL validation. Session 85 spent 6 rounds without device visibility = no closure; session 86 used Inspector + 1 fix commit + 1 cleanup = bug closed in ~30min. Top-of-list Tech Rule promotion candidate. **Promotion-ready.**
- **VALIDATION (session 86): "Match the visibility tool to the bug's layer"** — promoted via 3rd firing of `feedback_visibility_tools_first.md`. The Storage-panel-revealed-zero-writes is the canonical case study. **Promotion-ready.**
- **VALIDATION (session 86): `feedback_kill_bug_class_after_3_patches.md`** — 2nd clean firing. Rule predicted "structural fix < accumulated patches"; fix was 1 line of logic which retired -85 lines of session-85 speculation. **Promotion-ready.**
- **NEW: Frame C compact-photo alternative parked** (session 75) — [`docs/mockups/request-booth-redesign-v1.html`](docs/mockups/request-booth-redesign-v1.html) Frame C ready as a second pass if iPhone QA reveals 3:2 dropzone still feels overwhelming. CSS-only change.
- **NEW: Project-wide rule "letters → IM Fell or sans; numbers → FONT_NUMERAL"** (session 75) — captured in [`docs/booth-numeral-font-design.md`](docs/booth-numeral-font-design.md) carry-forwards. Promote to design-system.md on next consolidation.
- **NEW: BoothHero lightbox z-index sandwich pattern** (session 75) — transparent overlay at z-5 between photo container and pencil/post-it. Reusable when adding tap targets to affordance-rich surfaces. Single firing.
- **NEW: Long-form helper copy on paper-wash → FONT_SYS, not IM Fell italic** (session 75) — photo dropzone copy was the trigger. Single firing. Tech Rule on second firing.
- **NEW: Narrow-named tokens audited for generalization** (session 75) — `FONT_POSTIT_NUMERAL` discovery. Single firing.
- **NEW: Item 6 (session 76) — FB Marketplace shared-element transitions** carried into session 77. Mockup + framer-motion `layoutId` testbed + rollout across 4 thumbnail surfaces. Per `feedback_testbed_first_for_ai_unknowns.md`, build `/transition-test` testbed page first to validate cross-route shared-element transitions before touching real surfaces. Size M-L; likely its own session.
- **NEW: E6 (session 76) — scroll-restore safety deferred** — extract `useScrollReveal` from [`app/page.tsx`](app/page.tsx) into `hooks/useScrollReveal.ts`; generalize sessionStorage-key + ref machinery so Booths / Find Map / /flagged adopt skip-entrance-on-restore. ~30–60 min in its own session.
- **NEW: Frame C parked on find-title centering** (session 76) — [`docs/mockups/find-detail-title-center-v1.html`](docs/mockups/find-detail-title-center-v1.html) Frame C (22px italic subtitle) ready as CSS-only pivot if 32px twin reads heavy on real vendor content during seeding.
- **NEW: Sheet/modal entrance animation consistency NOT touched** (session 76) — DeleteBoothSheet, AddBoothSheet, EditBoothSheet, MallSheet, AddFindSheet, ShareBoothSheet etc. carry their own entrance animations. Inventory deferred to a separate consistency pass on overlay primitives.
- **NEW: `app/finds/page.tsx` (dark reseller layer "My Picks") deviates from motion tokens** (session 76) — out-of-scope by design (different theme, different layer). Future cleanup if dark layer ever migrates.
- **NEW: Home MasonryTile bespoke easing intentionally diverges** (session 76) — `cubic-bezier(0.22,1,0.36,1)` for transform + 0.38/0.44s split durations preserved as intentional polish on hero feed grid. Token values centralize but the runtime is forked.
- **NEW: Detail-page render branches must render base chrome in every state** (session 76) — first firing (find/[id] bug). Single firing; Tech Rule on second firing.
- **NEW: Default to stacked over inline for title + secondary value patterns** (session 76) — first firing (find-title em-dash retirement). Single firing.
- **NEW: Audit invariants against sibling pages of the same shape after 3+ recent sessions** (session 76) — first firing (find/[id] masthead invariant existed in /shelf/[slug] but not here despite 3 recent layout sessions). Single firing.
- **Feed content seeding** ✅ — **resolved pre-session 82** (David completed seeding before session 82 standup). 15 posts across 2 mall locations and 3 vendors. The 24× bumped item finally came off the queue. Real-content QA loop now possible.
- **Hero image upload size guard** — verify coverage across upload surfaces. Note: session 61 added DELETE coverage at `/api/admin/featured-image`; size guard remains a separate concern on the upload path.
- **Tech Rule promotion sharper-shape** ✅ — **resolved session 88 arc 2** in `8635487`. All 11 🟢 candidates promoted per `feedback_tech_rule_promotion_destination.md`: 5 new memory files for operating-style rules + 2 prose entries in `docs/DECISION_GATE.md` for production-safety rules + 4 marked ✅ Promoted-via-memory. Queue rebalanced to 0 🟢, 26 🟡 after 4 new firings (TR-ah/ai/aj + new total-strip memory).
- **Wordmark iPhone QA on Vercel preview** ✅ — **partially resolved session 88 arc 1** in `28d60ca`. Wordmark height 30 → 40px filling masthead's 40px inner grid; David's iPhone QA on Home approved. Full surface walk across /find/[id] + /shelf/[slug] + /my-shelf still untested but the height question is closed.
- **Wordmark asset-weight optimization** ✅ — **resolved session 104** via `npx -y pngquant-bin --quality=70-90 --speed=1`. 455KB → 107KB (76% reduction). Dimensions preserved 1500×800 so in-app render at 72px height stays crisp. Operational backlog item closed.
- **design-system.md wordmark spec stale** (session 87 carry, ~5 min). Likely references session-80 TNR-upright-18px-ink-black; add a one-line note on the new wordmark-as-image approach + the 40px height bumped at session 88.
- **NEW PRIMARY (session 88): motion design revisit in a focused full session.** David: "Lets just pull out all the animations for now and we'll revisit this in a full session. It still feels off." Ecosystem layer (Home + /flagged + /find/[id]) is now fully static motion-wise after `b3e47df`. The right next move is a fresh design pass with mockups (per `feedback_total_strip_after_iterative_refinement_fails.md`), structural alternatives spanning real options (no morph / minimal motion / full Track D revival), iPhone QA against real seeded content, ship one option. Size M-L, ~60–90 min.
- **NEW (session 88): iPhone QA verification of `01ff797` pushState monkey-patch on /find/[id] scroll restore.** David's last QA on the scroll arc was BEFORE the pushState fix landed. The "tap same find from Home twice" flow needs a fresh iPhone walk to confirm the staleness fix actually closes the bug. ~5 min if clean, ~15 min if more diagnosis.
- **NEW (session 88): preview-cache role audit.** `treehouse_find_preview:${id}` sessionStorage usage is reduced now that the `motion.div layoutId` photo morph is gone. Possibly removable, possibly still useful for sync first-paint of the photograph. ~15 min audit + decide.
- **NEW (session 88): full surface walk on the static state.** David's iPhone QA's after the total strip were partial (Home + /flagged + the back-nav case). A clean cold-start walk across all primary screens (Home, /flagged, /find/[id], /shelf/[slug], /my-shelf, /shelves, /vendor-request) on the static state would close the loop and provide the baseline for the motion-design revisit session. ~10 min.
- **iPhone QA verification of session-89 ship** (session 89 carry) — partially resolved session 90: David surfaced 7 issues from the prior preview ship + 4 follow-up items, all closed in 7 commits. Full multi-surface device walk still untested across the new state.
- **BoothHero pencil-vs-bookmark symmetry question** (session 89 carry) — UNTOUCHED session 90. Still awaiting clarification.
- **Sign-in/sign-out masthead primitive extraction candidate** ✅ — **RETIRED session 90**. Auth chrome moved off masthead onto BottomNav (Profile tab) + /login (sign-out italic link); the would-be 2-surface primitive (Home + /flagged) is dissolved. No further firings of this candidate.
- **/flagged tile density risk at 3-col on 390px-wide phone** (session 89 carry) — David didn't flag during session 90 either; presumably acceptable. Stays as a watch-item against real-content seeding.
- **Phosphor icon weight calibration** (session 89 carry) — single firing as of session 89; session 90 did not extend (IoKey replaces Shield in BottomNav but is from `react-icons/io5`, not Phosphor — different package; weight calibration there is a separate single-firing observation).
- **"Cross-page consistency overrides earlier 'browse vs navigate' rule"** (session 89 carry) — single firing, no further firings session 90.
- **iPhone QA verification of session-90 ship** (session 90 carry) — closed session 91/92 (no regressions surfaced across two intervening sessions of David QA).
- **IoKey weight on top of Lucide-stroked tab row** (session 90 carry) — UNTOUCHED session 91. Pending iPhone QA.
- **/find/[id] right-slot share redundant with masthead default** (session 90 carry) — UNTOUCHED session 91. Single firing; alternative next move slot.
- **Default-when-empty masthead pattern primitive-extraction candidate** (session 90 carry) — single firing as of session 90.
- **Sign-out hidden behind Profile tab tap discoverability** (session 90 carry) — UNTOUCHED session 91. Beta-feedback watch-item.
- **Decoupled icon vs label color in BottomNav** (session 90 carry) — single firing as of session 90.
- **`right={null}` "stay quiet" idiom on primary tabs** (session 90 carry) — single firing as of session 90.
- **Auto-redirect-suppression for already-authed users on `/login`** (session 90 carry) — single firing as of session 90.
- **🖐️ HITL migration 016 (R11 mall hero column)** ✅ — **resolved session 92** (David completed paste in both projects).
- **iPhone QA verification of Wave 1 ship** ✅ — **resolved session 92** (David ran the walk + reported "QA walk passed" with 3 design-level redirects, NOT bugs — those redirects became commits `8ec7884`, `4b515b2`, `b6919b7` this session).
- **Wave 1.5 security continuation** ✅ — **resolved session 92** end-to-end: `/api/vendor-hero` + `/api/post-image` auth+ownership tightened, multi-booth ownership bug fixed in `/api/my-posts/[id]`, orphan `/api/debug` deleted, function search_path locked (migration 017 + diagnostic), role-grant drift diagnostic shipped (migration 018 + diagnostic), OTP/password manual procedure documented + dashboard toggles applied, AI-route auth deferred per option (c). All 6 sub-tasks closed. R1 hard prereqs all green.
- **NEW (session 91, carry to session 93): `vendor_request_approved` admin actor lacking `_by_admin` suffix.** Naming inconsistency preserved per copy-scrub rule. Cosmetic; defer to Q-014 Metabase scoping.
- **NEW (session 91): 9 new event types added** (plus `vendor_hero_removed` extended in 92 with `by_admin` flag). R3 events table volume worth mentioning at Q-014 Metabase scoping.
- **NEW (session 91, carry to session 93): feed pagination still deferred.** Revisit when feed crosses ~100 posts.
- **NEW (session 91, carry to session 93): Q-015 admin Vendors tab captured but not pulled.** Single-pane-of-glass argument lives in `docs/queued-sessions.md` §Q-015.
- **NEW (session 91): Honest sprint-brief reassessment when off-roadmap items get flagged then dropped.** Single firing. Tech Rule on second.
- **NEW (session 91): Discovery-during-execution → reframe + capture.** R4a discovery during Task 3. Single firing.
- **NEW (session 91): Mockup token-source verification.** `#f5f2eb` vs `v1.paperCream = #e8ddc7` mockup misuse. Tech Rule candidate; second firing on next mismatch.
- **NEW (session 91): Migration + impl can ship in adjacent commits when the column reads as optional in TS.** Pattern only works when TS layer pre-declared the field.
- **NEW PRIMARY (session 92): iPhone QA of the 3 Wave 1 follow-up commits.** Contact email split (`/contact` mailto + `lib/email.ts` replyTo), mall hero relocation BELOW MallScopeHeader at FeaturedBanner dimensions, EditBoothSheet hero consolidation (BoothHero clean + Booth photo section in sheet, both vendor + admin modes). David hasn't device-walked the refinements yet.
- **NEW (session 92): `inspect-grants.ts` heuristic refinement.** Diagnostic flagged 7 yellows on prod / 6 on staging — all confirmed false positives via separate `inspect-rls.ts` cross-reference (Supabase ships every public table with full DML grants to anon + authenticated by default; RLS does the actual gating). Diagnostic should cross-reference `audit_rls_state()` and only flag write-grants when RLS is ALSO disabled. ~20 min in next session.
- **NEW (session 92): Migration 010 + 011 staging gap.** `events` table missing on staging — known carry from session 58. Apply when staging needs to mirror prod for analytics testing.
- **NEW (session 92): AI-route auth deferred** (option c). 6 routes (`extract-tag`, `identify`, `post-caption`, `report-comps`, `story`, `suggest`) deliberately unauthed because adding `requireAuth` would break the reseller-intel layer (`/scan`, `/decide`) which is `localStorage`-only by design. Runbook captures revisit triggers (general beta traffic OR cost spikes) + Upstash Redis as the upgrade path.
- **NEW (session 92): Surface auth/identity conflation issues BEFORE editing.** First firing — caught the `NEXT_PUBLIC_ADMIN_EMAIL` conflation (admin gating identity vs. public-facing contact email). If David had said yes to the first plan, he'd have lost admin access. Tech Rule on second firing.
- **NEW (session 92): Multi-booth `maybeSingle()` ownership bug pattern.** `.eq("user_id", x).maybeSingle()` errors when user owns >1 booth. Real bug found in `/api/my-posts/[id]` PATCH; fixed via composite filter. Tech Rule on second firing — anywhere ownership probes hit `vendors` table by `user_id`.
- **NEW (session 92): Diagnostic heuristic must cross-reference RLS state.** First firing — `inspect-grants.ts` flagged Supabase-default architecture as 🟡 noise. Future security diagnostics that operate on grant/policy/role layers need cross-axis awareness. Tech Rule candidate; refinement is the next-session item.
- **NEW (session 92): Reuse existing primitive when dimensions match request.** First firing — mall hero went from bespoke 26-line block to 1-line `<FeaturedBanner variant="eyebrow">` reuse. Pattern: when a request asks for "X dimensions," check if an existing primitive already provides them before re-implementing. Tech Rule on second firing.
- **NEW PRIMARY (session 93): iPhone QA of Wave 1 follow-up commits still owed.** David QA'd /login triage flow this session but session-92 follow-up surfaces (contact mailto, mall hero composition, EditBoothSheet hero section, /shelves admin EditBoothSheet hero controls) remain un-walked. Watch-items per session 92 close still apply.
- **NEW (session 93): R1 (shopper accounts) routing slot pre-baked.** Login routing model anticipates the R1 shape: `/login` triage page has 2 cards today; R1 adds a 3rd "I'm shopping" card. `/login/email` Screen 2 title "Vendor Sign in" generalizes to "Sign in" when R1 ships. The "Just shopping?" guest disambiguation line on /login retires (replaced by 3rd card). Subtitle generalizes. All 4 changes contained to `app/login/page.tsx` + `app/login/email/page.tsx`.
- **NEW (session 93): /admin/login + /setup `handleGoToLogin` callsites left routing through triage.** Bare `/login` calls — go through triage cards. If user testing reveals friction (extra tap to reach the form for what feels like a known returning user), single-line update each to point at `/login/email` directly.
- **NEW (session 93): Triage-first routing for ambiguous-state pages.** First firing — `/login` had to handle 4 user states (returning vendor / new vendor / authed / magic-link callback) which a single form could not disambiguate. Pattern: when a page must serve genuinely-different user states, surface the disambiguation in routing rather than letting one component conditionally render. Tech Rule on second firing.
- **NEW (session 93): Inbound deep-link audit when splitting a route.** First firing — when changing route topology (one route → two routes), grep all inbound references and decide per-callsite whether to update or rely on forwarding. /vendor-request "Sign in instead" + /my-shelf re-auth bounce got per-callsite upgrades. Tech Rule on second firing.
- **NEW (session 93): Forward-known-states + render-fresh-states routing primitive.** First firing — `/login` triage state machine: if `?confirmed=1` → render polling, if explicit redirect → forward, if authed → forward, else → render triage cards. Pattern: render only states that need fresh interaction; forward known states to their canonical destination. Tech Rule on second firing.
- **`/shelf/[slug]` 404 NotFound icon still a `<Heart>`** — session 61 swap intentionally left this one alone (semantically unrelated to "save"). Flip to a different icon if a separate visual sweep happens.
- **Tag-extraction analytics events** ✅ — **resolved session 73**. `tag_extracted` (with `has_price` + `has_title`) + `tag_skipped` shipped.
- **Sign-in icon discoverability** (session 63 watch-item) — first-time guests see a 22px CircleUser glyph in the masthead right slot. Worth checking during beta-feedback.
- **Q-012 — Treehouse opener animation** (session 64, parked) — full Design redesign needed. Captured in [`docs/queued-sessions.md`](docs/queued-sessions.md) §Q-012.
- **Existing `/api/*` routes are unwrapped re Sentry** (session 65) — ~25 routes silently swallow errors at the framework level. Acceptable for now; auto-fixed by Q-013.
- **Q-013 candidate — Next.js 15 upgrade** (session 65, parked) — would auto-fix the Sentry wrap-route requirement and unlock Cache Components.
- **Sentry MCP not yet wired to Claude Code** (session 65 follow-up) — one-time fix: add a `.mcp.json` block.
- **`/mall/[slug]` lensless on v0.2 dark theme** (session 66) — known deviation; lens applies cleanly when mall page does its v1.x ecosystem migration.
- **Treehouse Lens constant is provisional** (session 66) — `contrast(1.08) saturate(1.05) sepia(0.05)`; real vendor content this week may reveal it needs tuning.
- **Booth-bookmark analytics events** ✅ — **resolved session 73**. `booth_bookmarked` + `booth_unbookmarked` with `{vendor_slug}` payload.
- **Spec deviation: admin /shelves tiles don't carry the bookmark bubble** (session 67) — Pencil + Trash already in top-right; admin can bookmark via `/shelf/[slug]` masthead. Reconsider only if admins want it.
- **Mall-filter analytics events on Booths + Find Map** ✅ — **resolved session 73**. `filter_applied` fires on all three primary tabs with `page` field for per-tab attribution.
- **`/find/[id]` cartographic visual asymmetry** ✅ — **resolved session 70 + 71** via cartographic collapse.
- **`/find/[id]` → `/mall/[slug]` link bridging warm-→-dark cliff** ✅ — **resolved session 71** via collapse.
- **Card-height variance on find tiles** ✅ — **resolved session 71** via caption `minHeight`.
- **BoothHero post-it `minHeight: 96`** (carry from session 69) — eyebrow now single-line "Booth"; post-it may look slightly tall. If iPhone QA flags it, the parameter at [`components/BoothPage.tsx:216`](components/BoothPage.tsx) can come down (~76 likely tighter).
- **D4b session-46 reversal needs real-photo validation** (carry from session 69) — IM Fell 14px on ~170px-wide booth-detail/find-detail tiles tested against synthetic seed photos; real vendor content during the seeding walk is the actual judgment moment.
- **Design-record price-color token deviation** (carry from session 69) — design record specified `v1.inkMid`; implementation kept `v1.priceInk` (established semantic token).
- **`v1.pillBg` / `v1.pillBorder` / `v1.pillInk` tokens** ✅ — **resolved session 70**.
- **All-malls scope mall-subtitle is a weak separator on `/flagged`** (session 70) — captured in Record 1 design record as known-limitation. Likely fix: mall-grouped section breaks with small all-caps mall eyebrow.
- **Booth-page scroll flatten (D17) regression watch** (session 70) — highest-risk decision in Record 3. Watch on iPhone QA.
- **`/post/preview` masthead unification — title-below-masthead pending iPhone QA** (session 70) — page may lose the "reviewing" feel against vendors stepping through Add Find. (Note: session 74 touched /post/preview for item 4 but didn't change title placement.)
- **Booths grid lockup numeral size deviates from spec** (session 70) — design record specified 26px; Booths grid implements at 20px to preserve tile proportions. Design-record L3 amended.
- **Item 3 — animation consistency review across pages** ✅ — **resolved session 76** in `ecb8ef8` + `3832fc1`. 5 motion tokens centralized in lib/tokens.ts; Booths VendorCard tap parity shipped; empty states unified. E6 (scroll-restore safety) deferred separately.
- **Item 5 follow-up: B + A parked** (session 71) — `object-position: center top` (B) + slot-ratio flip 4/5 → 1/1 (A) stay parked unless real-content seeding reveals residual photo-cropping issues. Mockup at [`docs/mockups/image-standardization-v1.html`](docs/mockups/image-standardization-v1.html) ready as second pass.
- **Item 6 — shareable filtered-mall link** (session 71) — needs product + URL-param scoping + share UX + mockup pass.
- **Sub-agent claim verification gap** (session 71) — single firing; Tech Rule candidate on second firing.
- **Screenshot-before-scoping discipline for visual fixes** (session 71) — single firing; Tech Rule candidate on second firing.
- **Multi-affordance retirement → single-surface promotion** (session 72) — single firing in admin context, single firing in shopper context — Tech Rule candidate on a third firing.
- **Spec deviation: admin /shelves masthead right slot is now empty** (session 72) — empty-on-purpose is a valid state.
- **Q-014 — Metabase analytics surface + retire admin Events tab** (David surfaced session 73) — strategic next step on the analytics arc. Captured in [`docs/queued-sessions.md`](docs/queued-sessions.md) §Q-014.
- **Diag strip + raw probe stay as durable visibility tooling** (session 73 decision) — retire automatically when Q-014 lands and admin tab retires.
- **Other supabase-js call sites might have the same latent bug** (session 73) — audit candidates: any inline `createClient` outside `getServiceClient()`. Quick grep: `grep -rn "createClient" lib/ app/`. Not urgent.
- **NEW: Hero photo asymmetry between Add and Edit sheets** (session 74) — Add sheet has hero photo upload; Edit sheet doesn't (per D5, /my-shelf?vendor=<id> is canonical hero-edit surface). Easy expand if asked.
- **NEW: AddBoothInline retained on /admin Vendors tab** (session 74) — duplicate Add UI exists (AddBoothSheet on /shelves + AddBoothInline on /admin). Field shapes match; future cleanup may unify if drift surfaces.
- **NEW: /vendor/[slug] no longer reachable from /shelves Pencil** (session 74) — intentional per D1. Reachable from `/shelf/[slug]` and direct nav. Add a "View profile" eyebrow on EditBoothSheet if surfaced.
- **NEW: Slug-collision shares the BOOTH_CONFLICT path** (session 74) — generic error message says "booth number" but actual cause might be slug uniqueness. Add slug-specific path if surfaced.
- **NEW: Third oscillation of AddBoothInline-on-/shelves** (session 74) — captured in [`docs/booth-management-design.md`](docs/booth-management-design.md) Carry-forward so future sessions don't reflexively retire.
- **NEW: `showPlaceholders` prop infrastructure dormant in BoothPage** (session 74) — unreachable after item 6 dropped the only `showPlaceholders={true}` callsite. ~10 min cleanup pass to reap; deferred.
- **Archive-drift backfill** — tombstones for sessions 54 + 55 + 57–73 missing from `docs/session-archive.md`. Pure docs-housekeeping. Worth a single ~15min ops pass at some point.
- **Operational backlog** — staging migration paste, OTP template paste, debug-toast cleanup, doc-only updates (Design agent principle, KNOWN PLATFORM GOTCHAS), `/admin` `auth.users` delete spike, `/api/suggest` SDK migration, archive-drift backfill (sessions 54 + 55 + 57–85 missing), strip verbose diag logs from `/api/admin/events/route.ts`, audit other `createClient` callsites, reap dormant showPlaceholders prop infrastructure in BoothPage.tsx, **session 80: tidy V1 mockup files** ([booths-tile-redesign-v1.html](docs/mockups/booths-tile-redesign-v1.html) + [wordmark-redesign-v1.html](docs/mockups/wordmark-redesign-v1.html), superseded by V2), **session 80: amend [`docs/marketplace-transitions-design.md`](docs/marketplace-transitions-design.md) with "Source side retired on /shelves" addendum**, **NEW (session 81): Q-014 Metabase planning** — decide whether to rename `mall_activated`/`mall_deactivated` event keys to `location_*` (backwards-compat migration, not flag-day), **NEW (session 81): consider `v1.cartographicBg` semantic token extraction** if more cartographic-card surfaces accumulate, **NEW (session 86): aggressive cleanup of /my-shelf scroll-restore code** — session 85's structural patches (BFCache pageshow handler, popstate, useSearchParams reactivity, dual-write storage, retry-scrollTo loop) may all be vestigial now that root cause is fixed at the write site; revert to /flagged-style minimum viable code, ship, run iPhone QA across the 3 Phase 3 surfaces, retain simpler version if clean. ~20 min standalone. → [`docs/queued-sessions.md`](docs/queued-sessions.md) §Operational backlog.
- **Error monitoring (R12)** ✅ — **shipped session 65** end-to-end. Sentry SDK + Vercel-Sentry integration + source-map upload + PII scrubbing + email alerts. Dashboard: https://zen-forged.sentry.io/issues/?project=4511286908878848.
- **Beta feedback mechanism** — absorbed into R7 (contact us) as a sub-task.

### 🟡 Q-007 Window Sprint expansion (post-MVP)

All captured in `docs/queued-sessions.md`:
- **Q-008** ✅ — Open share to unauthenticated shoppers. **Shipped session 50; QA walk PASSED session 51.**
- **Q-009** ✅ — Admin can share any booth (ownership bypass). **Shipped session 45.**
- **Q-010** ✅ — Window email CTA URL fix (`/vendor/` → `/shelf/`). Shipped session 41.
- **Q-011** ✅ — Window email redesign. Shipped session 52 over four iterations; v4 on-device QA walk **PASSED 4/4 clients session 53**. Loop fully closed.
- **Q-006** 🟡 — Deep-link CTA. Parked on Sprint 6+ Universal Links.

### 🟡 Session 35 non-gating follow-up

- **Q-002** ✅ — Picker affordance placement revision. Shipped session 57.

### 🟡 Sprint 5 + design follow-ons

- **`/admin` v0.2 → v1.2 redesign pass** (session 37) — Posts tab, Banners tab, tab switcher, header, approval toast, diagnosis panel. Size L. Needs design scoping first (mockup-first per session-28 rule). ~2–3 sessions.
- **KI-005** — Pre-approval sign-in signaling gap. Batch with guest-user UX.
- **Typography reassessment** (session-32 deferral — IM Fell readability).
- **Post-approval booth-name edit surface** (session-32 deferral). **Note: session 74 partially addressed this for admin** via the new EditBoothSheet, which any admin can use to edit any booth's name. Vendor-self-edit is still parked (vendors can't edit their own booth name yet; only admins can).
- `<MallSheet>` migration to `/vendor-request` (still deferred per v1.1k (h)).
- Nav Shelf decision + BottomNav full chrome rework.
- Guest-user UX parked items: login page now reads "Vendor Sign in" (session-49); `/welcome` guest landing, PWA install onboarding, vendor onboarding Loom, bookmarks persistence still parked.
- **PWA pull-to-refresh** — confirmed missing in session 41 Q-007 walk. Sprint 5 polish.

### 🟢 Sprint 6+ (parked)

**Primary roadmap now lives in [`docs/roadmap-beta-plus.md`](docs/roadmap-beta-plus.md)** (session 55 capture — 15 epic-level items R1–R15 including guest profiles, Stripe subscriptions, analytics, admin tooling, feed caps, legal, onboarding, push/SMS, map nav, mall heros, error monitoring, mall-operator accounts, vendor profile enrichment, **app store launch**). Roadmap doc also carries the 8-cluster grouping + 3-horizon shipping plan.

**Still Sprint 6+ only (not in roadmap-beta-plus.md):**

QR-code approval, admin-cleanup tool (session 45 materially reduces the need; session 74 closes the booth-name + mall-edit gap; the 80% case is now covered by /shelves), mall vendor CTA, **Option B `vendor_memberships` migration**, **Direction A (link-share via native share sheet)** and **Direction C (PNG export / story-ready image)** share variants. Post-MVP: 3A sold landing state, Find Map saved-but-sold tile signal.

### 🟢 Cleanup (not urgent)

- Deprecated functions in `lib/posts.ts` including session-35 `getVendorByUserId` shim.
- Cloudflare nameservers dormant (no cost).
- `docs/VENDOR_SETUP_EMAIL_TEMPLATE.md` (obsolete since T4a).
- `docs/design-system-v1.2-draft.md` (tombstone).
- `docs/mockups/add-find-sheet-v1-2.html`, `review-page-v1-2.html`, `edit-listing-v1-2.html` — partial retirement pending post-beta.
- `docs/mockups/email-v1-2.html` — updated session 39 rename sweep; keep as reference.
- `docs/mockups/share-booth-email-v1.html` through `v4.html` — design-history reference for Q-011 arc.
- `docs/mockups/my-shelf-multi-booth-v1.html` — Frames 2 + 3 updated session 57 (Q-002).
- `docs/multi-booth-build-spec.md` — archived reference.
- `docs/share-booth-build-spec.md` — Q-011 addendums stacked. Post-QA, candidate for consolidation.
- `components/ShelfGrid.tsx` (parked; zero callers).
- `/post` redirect shim — can delete post-beta.
- `.env.prod-dump.local` (session-54 one-shot artifact, gitignored, safe to delete anytime).
- Orphaned `dbutler80020@gmail.com` staging auth user (non-admin, non-blocking).
- **NEW: showPlaceholders prop infrastructure in BoothPage.tsx** (session 74) — unreachable after `/my-shelf` dropped the only `showPlaceholders={true}` callsite. ~10 min cleanup.

---

## INVESTOR UPDATE SYSTEM

- Google Drive folder: `1l2toRdb-1sKCuYcJ25OKYzMqNMu2kBWW`
- Cadence: end of each sprint (weekly once beta launches)
- Trigger: say "generate investor update" at session close
- Process doc: Notion → Agent System Operating Manual → 📋 Investor Update — Process & Cadence

> **Sprint 4 fully closed sessions 40–41; sessions 42–53 ran the pre-beta polish arc; sessions 54–69 carried the polish + observability + brand + navigation + persistence + terminology + caption-typography + Option B booth-identity sweep; session 70 was the largest layout-redesign sweep in recent memory; session 71 closed three independent decision arcs; session 72 rationalized admin entry; session 73 closed R3 end-to-end; session 74 closed the Gemba-walk admin-management gap; sessions 75 + 76 closed David-redirect bundles end-to-end; session 77 shipped Track D phases 1–4 + the masthead 5-attempt arc; session 78 shipped Track D phase 5 end-to-end. Session 79 was net-zero (Track D extension attempt reverted). Session 80 closed a David-redirect bundle (5 items + 2 polish). Session 81 closed the demo-prep refinement bundle. Session 82 closed the largest design-system consolidation pass since the v1.x layer was named. Session 83 closed the polaroid evolved tile direction end-to-end. Session 84 was the first pure-security session since R12 Sentry. Session 85 closed Phase 3 scroll-restore on 2 of 3 surfaces. Session 86 closed the third — /my-shelf admin scroll-restore — in 1 fix commit + 1 cleanup after Safari Web Inspector via USB connected. Session 87 reconciled the Tech Rule queue (17 → 33 candidates) and shipped the brand asset overhaul. Session 88 closed the Tech Rule promotion batch end-to-end + bumped the wordmark height + ran a 9-commit animation/scroll-restore arc that ended in David's "pull out all the animations" call. Session 89 was a design intentionality pass — 9 runtime commits across two arcs. Session 90 was the auth-chrome relocation pass — 7 runtime commits relocating auth chrome off masthead onto BottomNav Profile tab + /login. Session 91 was the strategic-reset + Wave 1 cleanup pass — 13 runtime commits, roadmap moved 3/15 → 8/15. Session 92 was the Wave 1 follow-up + Wave 1.5 security continuation — 11 runtime commits across two arcs; R1 shopper accounts FULLY UNBLOCKED. Session 93 was the login triage cleanup — 1 runtime commit splitting `/login` into triage + `/login/email` for the OTP form. Session 94 was the capture-and-add-find UX refinement — 4 runtime commits with masthead +40 + wordmark 90 system-wide. Session 95 was the design system audit + Phase 2 A+B — 13 runtime commits across Phase 1 audit + Phase 2 plan + Session A token foundation + Session B `<PolaroidTile>` primitive (6 callsites, ~250 lines deleted) + cross-session login redirect fix + wordmark trim 90→72. Session 96 was the largest Phase 2 push of the project — 23 runtime commits across Sessions C+D+E end-to-end + F partial (EmptyState + FormField/FormButton + palette migrations + IM Fell comment rot strip). Session 97 was the iPhone QA followup + Edit your find layout restructure + hero bubble proportionality — 9 runtime commits across 3 arcs (6-item QA bundle + /find/[id]/edit layout restructure mirroring /post/preview + vendor/admin chrome retire promoted-via-memory + hero save bubble 44/22 dial). Session 98 was the capture-flow polish + flow-continuity system rule + form-submit conversion on /find/[id]/edit — 16 runtime commits across 3 arcs. Session 99 was the /flagged destination redesign + product-state.md scaffold + reference-first process change + thesis articulation — 4 runtime commits. Session 100 was the /find/[id] navigation refinement — 18 runtime commits across two phases + 8 QA cycles. Final flow: tap any tile → /find/[id] paints fully on first frame; drag horizontally → swipe to prev/next find with no history growth; back → exact original feed scroll position. Session 101 was the iPhone QA pass on session-100 + Path-4 Chrome/PWA back-nav bug + scroll-restore flash kill — 6 runtime commits across 3 arcs. **Session 102 was the R16 discovery search scope-to-Ready + Task 4 primitive ship — 3 runtime commits across 3 arcs. Arc 1: dependency mapping (search + pagination + image-cataloging = one product question; reseller `/api/identify` schema over-designed; right cut = enrich `/api/post-caption` Sonnet vision call with 6-axis subset, no new AI route, ~+150 tokens). Arc 2: reference-first → V1 (3-frame "Browse by..." landing page REJECTED) → David pivot ("minimalistic magic — search bar at top of feed page") → V1.5 search bar (3 states) → V2 with PiBinocularsFill ("more treehouse branded and scope" — composes with leaf brand mark + digital-to-physical thesis). NEW MEMORY captured: feedback_minimalistic_magic_no_destination_for_one_input. Arc 3: scope (commit `e1320e3` — design record + roadmap entry; R16 first R-item to graduate same-session 🟡→🟢) + Task 4 ship (`1faa2b8` — `<SearchBar>` primitive + `/search-bar-test` smoke-test in isolation, no consumers wired) + slim-down dial (`2fb3264` — pill shape, height 50→40px, after iPhone QA flagged "chunky"). David: "looks good."** Smallest→largest commit sequencing now **42+ firings** total, promoted-via-memory at session 88. Mid-session iPhone QA on Vercel preview now **~13 firings**, also promoted-via-memory. Reference-first memory at 2nd firing — Tech Rule promotion candidate at 3rd. V2-as-fill-refinement at ~2nd firing — promotion-ready. **Next natural investor-update trigger point** is now after R16 implementation lands — Phase 2 sits at 5/6 complete with primitive layer fully shipped, R16 will ship the data-layer primitive (post-tags + tsvector + search bar + AI enrichment of post-caption) that R10 (map), R5b (caps), and any future "trending tags" feature can reuse. R16 also closes the "users want to find specific things" beta-tester ask in a single sprint, with the digital-to-physical bridge thesis sharpening the narrative ("we don't search the database — we scout the landscape, with binoculars on a glass bar"). Sessions 100 + 101 + 102 together are a meaningful investor beat: "the app feels physical not page-y, the diagnosis discipline scales, and the discovery layer ships in one focused session because the design philosophy is articulated and memory-backed." **Session 103 was a small redirect ship — 1 runtime commit landing scripts/add-mall.ts + Copper Awning Flea Market in prod malls table, status draft. Future mall additions are now scripted (Google Maps URL → reverse-geocoded row) instead of hand-edited paste; the "standard simplified way" David asked for is in place. First positive firing of script-first-over-SQL-dump-first since session 57 promotion. Net change toward investor narrative: small but cumulative — every recurring DB operation now becomes a committed tool, not a chat-only paste, which is the same posture that drove the session 86 Inspector setup, the seed-staging script, and the security-audit runbook. The compounding shape is what matters here, not the single mall row.** **Session 104 was the brand-asset + email/auth UX polish bundle — 8 runtime commits across 3 arcs.** **Session 105 was the R16 implementation sprint end-to-end + custom-caret bug-class kill + backfill polish — 17 runtime commits across 4 arcs. R16 ✅ Shipped: discovery search closes the "users want to find specific things in a growing feed" beta-tester ask in one focused session, with title + tag + caption full-text search backed by a generated tsvector + GIN index (sub-50ms even at 10k rows). Tags get extracted by the same Sonnet vision call that already writes title + caption — zero vendor effort, zero shopper learning curve. The custom-caret bug saga was textbook visibility-tools-first + kill-the-bug-class: 4 CSS-only attempts failed (cap-speculative-patching memory escalated us at round 4), debug-overlay measurement showed the lever was disconnected, structural fix replaced the native iOS WebKit empty-input caret with a custom 2×17px brand-green vertical bar tracking the input center exactly. Three design-record reversals (D2/D5/D12) surfaced post-iPhone-QA + applied with explicit acknowledgement per the session-104 NEW memory feedback_surface_locked_design_reversals (now at 5 firings cumulative across 2 sessions — promotion-strength). Backfill caught Claude returning "subject: bee" axis-prefixed values; sanitizer split-on-colon fix mirrored across both backfill script + live route. Net change toward investor narrative: the discovery story is now coherent end-to-end — vendor publishes a find → Sonnet extracts 5–6 tags invisibly → shopper opens the app → types "brass" → sees brass items at the picked location. The digital-to-physical thesis is sharpest yet: "we don't search the database — we scout the landscape, with binoculars on a glass bar." 8/15 roadmap items shipped now (R4a/b/c, R5a, R7, R11, R12, R16). R1 (shopper accounts) is the next biggest unblocked move + sharpens the thesis further — saved finds + scout-history persisting across devices, not just per-localStorage. Session 105 also validated three operational disciplines that compound with project age: (a) cap-speculative-patching at round 3 — escalate to visibility tools (saved cycles in caret saga vs session-100's 8-round grind), (b) surface design-record reversals explicitly before shipping (preserves intent capture across multi-month design records), (c) kill the bug class after 3 patches — replace the underlying primitive structurally rather than patching iteratively (custom caret as the canonical pattern). Each compounding disciplines is now memory-backed and validated across 5+ firings.** **Session 106 was the R10 location map design pass — 0 runtime commits, 3 docs commits across 7 arcs (V1–V5 mockup chain + design-record locks). R10 graduates 🟡 Captured → 🟢 Ready in single session — fastest 🟡→🟢 graduation paired with deepest mockup chain to date (5 iterations). 27 frozen decisions D1–D27 + 2 component contracts + 11 sub-task implementation sequencing.** **Session 107 was the R10 implementation sprint Arc 1 + Arc 2 end-to-end — 12 runtime commits across 2 arcs + a follow-up iPhone-QA dial round. Arc 1 closed the primitive layer in single session (provider scoping memo + Mapbox picked + token plumbed; tokens shipped; three primitives shipped — `<PostcardMallCard>` V4-verbatim, `<PinCallout>` peek-state floater, `<TabPageMasthead>` inline 86px wordmark; `/postcard-test` smoke page). Arc 2 closed the consumer wiring in same session (skeleton `/map` route, BottomNav 4-tab flat redesign, `/login/email` authed-state cards, Home + Saved consumer wiring). David's QA round surfaced 4 asks → 5 design-record reversals across 2 records (R10 D19 partial / D20 / D27, R16 D3 / D4) — `feedback_surface_locked_design_reversals` now at 10+ cumulative firings across 4 sessions. NEW MEMORY captured: `feedback_lora_lineheight_minimum_for_clamp.md` (3rd firing of the rule, generalized beyond ≤14px to ANY clamped Lora). Net change toward investor narrative: the postcard-mall-card vocabulary + 4-tab flat nav are now LIVE on the staging surfaces. Tap the Map tab and a card with a postal stamp + wavy ink cancellation tells you where you're shopping ("from: America's Antique Mall") — the digital-to-physical bridge thesis is now visible chrome, not just a memory file claim. Mall scope unifies across Home / Map / Saved (change-only-on-Map) — the user has ONE persistent place identity that follows them through the discovery flow. R10 row is partial-graduated: Arc 1 + Arc 2 ✅ shipped, Arc 3 (Mapbox custom-styled map) + Arc 4 (cleanup) remain. Arc 3 deserves its own session per the design record. Roadmap snapshot unchanged at row level: 8/16 ✅ Shipped, 2/16 🟢 Ready (R3, R10 — partial), 6/16 🟡 Captured; promotes to 9/16 ✅ when Arc 3 + 4 close. **Next natural investor-update trigger point** is after Arc 3 + Arc 4 land — likely sessions 108–109. The compounding investor shape from sessions 105 + 106 + 107: design discipline (5-iteration mockup chain → locked record → primitive-isolated smoke test → 2 consumer-wiring arcs → 5 mid-flight design-record reversals captured with traceability) is now memory-backed and reproducible. Compounding disciplines validated this session: (a) reverse-locked-decision capture (10+ firings across 4 sessions), (b) Lora descender clearance under clamp (3rd firing → memory file), (c) smallest→largest commit sequencing (50+ cumulative firings). Each is a small piece of operational reliability that compounds with project age.** **Session 108 was the R10 implementation sprint Arc 3 + Arc 4 end-to-end — 7 runtime commits closing R10 ✅ Shipped (5 Arc 3 ship + 1 Arc 4 ship + 2 follow-up: 1 SSR/container/error-surface diagnostic + 1 zoom-fit dial). Mapbox custom-styled basemap shipped via runtime layer overrides on light-v11 — italic-Lora label face deferred until David moves to a Mapbox Studio-hosted style URL. Leaf-bubble pin markers via createRoot (same React vocabulary as FlagGlyph). Peek-then-commit interaction wired with PinCallout positioned via `map.project()` and re-anchored on pan/zoom/rotate. /shelves URL preserved as 301 redirect to /map (zero outbound callsites found in audit). David's first iPhone walk produced empty cream rectangle — diagnostic commit shipped 3 structural fixes in one (next/dynamic ssr:false + position:absolute container + map.on('error') visible surface) per `feedback_kill_bug_class_after_3_patches` extended to ambiguous-symptom debugging. Second walk loaded; surface dial tightened the all-Kentucky view from KY-rectangle to actual mall-extents. Net change toward investor narrative: **R10 ✅ Shipped end-to-end means the digital-to-physical bridge thesis is now visible chrome on the live PWA — open the app, tap Map, and you see Kentucky's active treehouse locations as leaf pins on a warm-cream cartographic basemap; tap a pin and a postcard-style callout reads "14 booths · 86 finds" with a chevron commit; tap commit and the map flies in + the persistent postcard mall card on every tab updates to that mall**. The discovery story stacks: vendor publishes a find → Sonnet extracts 5–6 tags → shopper opens app → searches "brass" at the picked location (R16) → location was picked on the map (R10). Roadmap: **9/16 ✅ Shipped (R4a/b/c, R5a, R7, R10, R11, R12, R16), 1/16 🟢 Ready (R3), 6/16 🟡 Captured.** First R-item ship since R16 at session 105. Compounding disciplines validated this session: (a) ambiguous-symptom-extension of kill-bug-class — 3 structural fixes in one diagnostic commit when round 1 doesn't localize the bug, instead of patching one cause per round (single firing today; tech-rule candidate on second), (b) PWA service-worker cache lag is a real failure mode on dependency-graph changes — `next/dynamic` ssr:false retroactively made the map resilient to it (single firing). Next natural investor-update trigger point is now after R1 (shopper accounts) lands — likely sessions 110–112. R1 is the next-biggest unblocked R-item + sharpens the digital-to-physical thesis further: identity that travels with the shopper across devices, not just per-localStorage.** **Sessions 109+110 were a combined chrome refinement + nav refinement + iPhone QA bug-fix bundle + scope-filtered badge — 16 runtime commits across 4 arcs, no roadmap movement (refinement is post-ship polish). Six R10 design-record reversals/extensions (D1 twice, D6, D13, D19 fully reversed, D26 extended) all surfaced explicitly with David's verbatim quotes per `feedback_surface_locked_design_reversals`. The biggest structural win: route-group + shared layout pattern (`app/(tabs)/layout.tsx`) eliminates tab-to-tab masthead+postcard flicker — chrome lives in layout, pages render only body, tab-to-tab nav doesn't remount. Nav reduced to 2-tab (Home + Saved) — Map becomes an interaction surface accessed via the postcard mall card; pin commit auto-routes to Home. Single contextual pill on /map: 'Clear' when scope set, 'Browse list' when all-Kentucky. Saved badge filters by mall scope (matches /flagged display). Auth chrome relocation pattern is now 3 migrations deep (≤87 right → 90 BottomNav-tab → 109 masthead-left) — each migration deliberate, each surfaced via the reversal-memory rule. Load-bearing fix: useSavedMallId now broadcasts a custom 'treehouse:saved_mall_change' window event on update + listens to storage events; the D9 mount-time-read-only assumption was invalidated by session-109's shared layout. Net change toward investor narrative: the location story has reached a clean stopping point — the chrome refactor consolidates *every* navigational surface into one masthead pattern with one navigation primitive (the postcard card) and a 2-tab nav for Home/Saved. The PWA now feels like one cohesive app, not a stack of pages with different chrome. The reversal discipline continues to compound: 8 reversals on R10's locked record across 3 refinement passes, each with traceable David-quote reasoning, zero silent regressions. Next investor-update trigger point is unchanged — after R1 ships (likely sessions 111–113).** **Session 111 was the R1 shopper-accounts design pass + Arcs 1–3 implementation in a single session — 8 runtime commits across schema/RLS + visual layer + auth/triage wiring + claim flow + sync footer + 1 design-to-Ready commit; 17 frozen decisions D1–D17 in `docs/r1-shopper-accounts-design.md`; reference-first scan of 6 cross-domain identity patterns landed B+E+G (lazy claim · quiet identity · saves only) + Frame B + option ii (Glass-shape `/me` reflective destination + masthead bubble swap to v1.green initials when authed). Migration 020 lands shoppers + shopper_saves + shopper_booth_bookmarks with composite PKs supporting ON CONFLICT DO NOTHING for D13 silent claim migration. End-to-end claim flow wired (visual partial — /me showed mock data pending Arc 4). 4 HITL warm-up carries closed (staging backfill / vendor-request QA / Copper Awning / migrations 009+010+011 paste). `feedback_reference_first_when_concept_unclear` 3rd cumulative firing → promotion-ready as Tech Rule.** **Session 112 was R1 Arc 4 read-side migration end-to-end — 5 runtime commits smallest→largest closing the data-layer half of R1. Two new hybrid hooks (`lib/useShopperSaves.ts` + `lib/useShopperBoothBookmarks.ts`) become the SINGLE SOURCE OF TRUTH for save/bookmark state across guest + authed paths. Three internal modes: guest → localStorage; authed + no shopper → localStorage; authed + shopper row → DB. Cross-instance sync via custom-event broadcast (idempotent via Set semantics so the toggling instance no-ops); cross-tab sync via storage events (guest path only — DB writes don't touch localStorage); optimistic toggle with DB-error rollback that REBROADCASTS the rollback as a fresh event so sibling instances don't drift. Idempotent UPSERT via composite PK supports double-tap and the silent-claim re-run mechanic. **5 save consumers wired (layout + /flagged + Home + /find/[id] + /my-shelf, net −83/+35 lines)**: page-local `loadFollowedIds` + sync useEffects + `loadFlaggedIds` + `pruneStaleBookmarks` + `removeBookmark` + visibilitychange handlers retire because the hook owns its listener fanout. **2 booth-bookmark consumers wired (/shelves + /shelf/[slug], net −42/+18 lines)**. **`/me` rewire (climax)**: mocks → real handle/initials/scoutingSince via useShopperAuth + counts via hook.ids.size + distinct mall_ids of saved posts; sign-out wired to supabase.auth.signOut() + router.push("/") with onAuthChange propagation reverting every live instance to localStorage state in the same render cycle. **End-to-end claim flow now wired on both sides** — guest saves → /login → "I'm shopping" → magic link → handle pick → silent migration → /me with real DB counts → sign out → localStorage restored. R1 graduates to ✅ Shipped on Arc 5 clean walk (likely session 113). NEW patterns (single firings, all Tech Rule candidates on second firing): hybrid hook abstraction over local + remote backing storage with reactive cross-instance event broadcast; hook abstraction reduces page-level state significantly when listener fanout moves into the hook; optimistic toggle with rebroadcast on rollback. Net change toward investor narrative: identity that travels with the shopper across devices is now live code — the digital-to-physical bridge thesis sharpens once more, with saved finds + scout-history + handle persisting via auth instead of device-bound localStorage. Compounding disciplines validated this session: (a) smallest→largest commit sequencing fired 5× (cumulative > 75); (b) two-step "isolated primitive ship → consumer wiring" pattern (commits 1+2 ship hooks alone, commits 3+4+5 wire consumers — same shape as session 102 SearchBar primitive + session 107 PostcardMallCard primitive); (c) tsc + npm run build green at every commit boundary so each commit is independently revertable. The compounding investor shape continues: each R-item closure stacks onto a project posture where the design philosophy is articulated, the operational disciplines are memory-backed, and the codebase reduces in size as it becomes more capable.** **Session 113 was the R1 Arc 5 end-to-end QA walk that surfaced 4 issues — 1 silent infra failure (migration 020 RLS partial-paste) + 3 design issues (signOut destination / shopper-only post-signin destination / missing Booth nav-bar icon for vendor/admin). 3 runtime commits closed the design issues smallest→largest; 1 HITL re-paste closed the infra. The infra discovery is a meaningful operational beat: `audit_rls_state()` showing `rls_enabled: true / policy_count: 0` is the silent-failure shape that makes service-role INSERTs feel green while authenticated SELECTs return `[]`. The session-92 NEW memory `feedback_diagnostic_heuristic_must_cross_reference_rls_state` predicted exactly this class of bug; Session 113 was its first production firing. Adding to operational disciplines validated this session: (a) `feedback_visibility_tools_first.md` — HAR via Safari Web Inspector localized the bug class in 1 cycle vs many speculative rounds; (b) `feedback_cap_speculative_patching_at_3_rounds.md` — escalated to visibility at Round 1 BEFORE any patching, cleanest firing yet; (c) `feedback_surface_locked_design_reversals.md` — 3 issues this session, all surfaced with the prior reasoning quoted before drafting code, cumulative >18 firings across 6+ sessions; (d) `feedback_smallest_to_largest_commit_sequencing.md` — 3 commits in canonical order, cumulative >78 firings; (e) `lib/auth.ts` `detectUserRole(user)` becomes the canonical post-sign-in role primitive — async, queries vendors + shoppers in parallel, returns `'admin' | 'vendor' | 'shopper' | 'none'`. Reusable for future role-aware chrome (BottomNav already consumes it for the Booth tab gate). Net change toward investor narrative: R1 Arc 5 was supposed to be a ~5 minute pure-verification walk; instead it surfaced a silent-infra bug that had been latent for 2 sessions, validated 4 operating-discipline memory files in production, shipped 3 well-shaped commits, and left the system in a more reliable + more reusable state than before. R1 graduates ✅ Shipped on the deployed `5cea49a` re-walk (likely session 114) — 10/15 R-items shipped, only R3 remains in 🟢 Ready. **Next natural investor-update trigger point** is now after R3 (or another Captured row) graduates — each remaining R-item is more strategic than tactical, so trigger cadence will likely be once-per-2-sessions rather than every session.** **Session 114 was the R1 Arc 5 re-walk + BottomNav refinement that closed R1 ✅ Shipped end-to-end — 1 runtime commit `cc06ae9` reordering the BottomNav role-tab from middle to rightmost slot + adding an Admin variant for the admin role. The walk passed 4/5; Step 5 partial-pass surfaced two redirects (admin-without-booth stranded + tab order reversal), both shipped in one commit. Per `feedback_surface_locked_design_reversals` (19th cumulative firing across 7+ sessions, promotion-strength validated), session 113's locked tab-order reasoning was surfaced verbatim before drafting any code; David's redirect was framed as a deliberate reversal, not a silent regression. Two NEW pattern firings worth watching: "stable persistent tabs in nav, not role-dependent positions" (Saved holds slot 2 in every variant; the role-specialty slot is rightmost) — generalizes to any future role-aware nav (`/admin` chrome, future R13 mall-operator nav); "verify role-conditional UI doesn't strand non-canonical role holders" — admin-without-booth was the silent-stranding case session 113 missed. R1 ✅ Shipped is the 10th R-item closed end-to-end — 4-session arc spanning design + Arcs 1–3 (111) → Arc 4 hybrid hooks (112) → Arc 5 walk + RLS partial-paste fix (113) → Arc 5 re-walk + BottomNav refinement (114). Net change toward investor narrative: identity that travels with the shopper across devices is now live + verified end-to-end. The "lazy claim · quiet identity · saves only" posture from session 111's reference-first design pass shipped exactly as scoped — vendor publishes a find → Sonnet extracts tags (R16) → shopper opens app + saves → footer prompts cross-device sync → magic link OTP → handle pick → /me reflective destination with real DB counts. Three compounding disciplines worth restating: design-record reversals tracked across 19 firings without silent regressions; smallest→largest commit sequencing across 78+ firings keeps each ship surgically revertable; reference-first design passes (3 cumulative firings since session 99) reduce concept-iteration cost dramatically. **Next natural investor-update trigger point** is unchanged: after R3 graduates (the only remaining 🟢 Ready row) OR after a 🟡 Captured row enters design phase. The compounding shape of the operating disciplines now matters more than the individual R-item ships — the system has become measurably easier to evolve safely as project age increases, which is the right shape for an early-stage product approaching beta.** **Session 115 was the login email-first redesign (Shape A) + admin-without-booth stranding fix end-to-end — 4 runtime commits + 1 docs commit. David opened with a 4-item polish recommendation; redirected to a product + design session on login triage. The design pass shipped reference-first → V1 (3 structural frames spanning the choice axis) → V2 (12 frozen decisions D1–D12 with David's verbatim copy) → 4-arc implementation smallest→isolated first → push → David's iPhone QA passed. Net runtime change is a DELETE (−983/+197) — /login/email's 800-line OTP form moved verbatim into /login, /login/email retired to a 33-line redirect alias. The codebase shrinks while the feature improves: one email field replaces three triage cards, server detects who you are via detectUserRole, fresh-auth users with no rows land on a new /welcome disambiguation card (vendor or shopper, first-sign-in only). Side-effect: Arc 1 fixes admin-without-booth stranded-state on /login (David's typical config) — action cards now query vendors row separately; `feedback_verify_role_conditional_doesnt_strand_non_canonical_role_holders` 2nd firing → Tech Rule promotion-ready on third. D3 (Apple/Google SSO) explored + deferred — Frame A's layout has natural headroom above the email card for additive SSO without redesign; double-work risk LOW. /login/email kept as redirect alias for ~30 days. /login surface stabilizes after 4 prior touches (49/90/93/113/114) + this consolidating refactor. **Net change toward investor narrative**: the auth-chrome relocation pattern that's been migrating across surfaces for 30+ sessions (right-slot → BottomNav tab → masthead-left → email-first form) reaches a clean stopping point. The discovery story (vendor publishes find → Sonnet extracts tags → shopper opens app → searches "brass" at picked location) now has a coherent identity-claim story attached to it — type email, magic-link verify, server figures out who you are, route to canonical destination. Compounding disciplines validated this session: (a) `feedback_reference_first_when_concept_unclear` 4th cumulative firing across sessions 99/102/111/115 — promotion-strength validated repeatedly; (b) `feedback_smallest_to_largest_commit_sequencing` 4 commits sequenced canonically (cumulative >82); (c) `feedback_v2_mockup_as_fill_refinement` fired (V1 spans structure, V2 locks fill); (d) `feedback_mockup_options_span_structural_axes` fired (V1 had 3 genuinely different shapes not style variants). Roadmap unchanged at row level (UX refactor not feature ship): 10 ✅ Shipped, 1 🟢 Ready (R3), 5 🟡 Captured. Next investor-update trigger point unchanged: after R3 graduates OR a 🟡 Captured row enters design phase. The "/login refactor as cumulative discipline showcase" beat is itself worth a paragraph in the next investor update — the design pass cost ~15 minutes (reference-first scan + V1 → V2), the implementation cost ~60 minutes (4 arcs smallest→isolated first), the push QA passed cleanly, and the codebase came out smaller AND more capable AND with a pre-existing bug class (admin-without-booth stranding) closed as a side-effect.** **Session 116 was the polish-bundle clearing + PostcardMallCard iPhone-QA refinement + dormant /shelves retirement — 11 runtime commits across 7 files (net +103/−737, ~634 net delete). The session opened with the original 4-item polish bundle (carry from session 108 → 110 → 113 → 115 → 116, four sessions deep) but David's review of the recommended bundle surfaced an additional 8-item list from his own iPhone-QA observations. Tiered triage (A pure-dial / B one-design-call / C structural-defer) kept the session focused as a single ship rather than a half-session start: Tier A + B + the original 4 items shipped this session; Tier C (back button on /flagged + heart flicker + badge flicker) deferred cleanly to session 117. The /shelves retirement (−701 lines) is the biggest single delete since session 115's redirect-alias retirement of /login/email — codebase continues its shrinkage trend while feature surface grows. Mid-session iPhone QA on Vercel preview produced 2 follow-up commits with David's verbatim values: cancellation SVG dialed to opacity 0.08 + heavy stamp-overlap (reads as watermark texture not competing element); eyebrow refactored from generic small-caps "SELECT LOCATION" to FONT_LORA italic "Location" right-aligned (mirrors the same-card "from:" eyebrow voice — the right reach was THIS-CARD existing voice, not SYSTEM-WIDE pattern). 3 NEW patterns observed (single firings, all Tech Rule candidates on second firing): "user-provided verbatim values ship as-is — don't relitigate the spec" (David's SVG plug-in); "eyebrow vocabulary should match same-card existing voice" (vs system-wide pull); "tier the ask before kicking off when scope expands mid-conversation" (12-item explosion → A/B/C triage kept session a single ship). **Net change toward investor narrative**: an operational beat about discipline at scale. The original 4-item carry was small but persistent — 4 sessions of mention without close. Session 116 cleared it AND absorbed an 8-item iPhone-QA expansion AND deferred the 3 structural items cleanly to a follow-up. The compounding disciplines (smallest→largest sequencing >93 cumulative; mid-session iPhone QA ~15th; surface-locked-reversals ~20th; Lora descender clearance 4th) all fired cleanly. The codebase came out 634 lines lighter with the dormant /shelves retired + the PostcardMallCard primitive iterated post-iPhone-QA. The next investor-update trigger remains unchanged: after R3 graduates OR a 🟡 Captured row enters design phase. But the "operational discipline at scale" narrative now has another data point — 8 sessions in a row of either ship-an-R-item or ship-a-polish-pass-without-regressions, which is the right posture for an early-stage product approaching beta.** **Session 117 was the R17 geolocation design pass + design-to-Ready (🟡 → 🟢 same-session) — 1 design-record commit `87f7a43` (+1650/−1) closing 21 decisions D1–D21 across V1 + V2 mockup chain. R17 — Geolocation-aware discovery is the second R-row to graduate same-day (after R16 at session 102), confirming the reference-first → V1 (structural axes) → V2 (fill-refinement axes only) pattern is now a 5-firing repeatable design-pass shape that compresses what would historically take 2-3 sessions of mockup iteration into a single focused session. David opened with /session-open recommending Tier C polish bundle; redirected to product+design session on geolocation enabling with reference mockup. Pre-V1 scoping questions surfaced 3 axes (surface scope / augment-vs-replace / permission UX) → David's answers locked frozen decisions. Reference-first scan of 6 cross-domain references (AllTrails as best thesis match — distance as the digital affordance pointing to a real place; Apple Maps native cards / Yelp / DoorDash / Facebook Marketplace / Eventbrite for permission-denial + iOS deep-link patterns) before any HTML mockup, per `feedback_reference_first_when_concept_unclear` 5th cumulative firing across sessions 99/102/111/115/117. V1 spans 3 structural frames (top-right pill + twin buttons / eyebrow-row pill + small-caps + primary CTA + text-link / inline pill + single combo CTA). David's V1 pick: hybrid "Frame A but with the distance pill shape of Frame B." Initial Claude V2 interpretation drafted with anchor A + typography B; David interrupted mid-draft with correct interpretation anchor B + CTAs A. NEW pattern surfaced + captured as memory file: "User-clarification mid-mockup-draft surfaces hidden interpretive gap — restate before continuing." Tech Rule candidate on second firing. V2 spans 3 fill-refinement frames on distance copy axis (α "2.7 MI" minimal / β "2.7 MI AWAY" verbose / γ "2.7 MI · 8 MIN" Apple-Maps-style pair requiring Mapbox Directions API). David's V2 pick: Frame α — postmark stamp; no API dependency. 21 decisions D1–D21 frozen in design record. Implementation infrastructure verified: all 29 active malls have lat/lng populated (session 103 add-mall.ts reverse-geocodes); /map?mall=<slug> from R10 (session 108) accepts mall scope; BoothLockupCard + BoothHero + PinCallout primitives augmentable without breaking changes. Implementation sequenced smallest→largest in design record: Arc 1 (one session, ~6 commits) primitives in isolation — lib/distance.ts haversine + lib/mapsDeepLink.ts UA-detection + useUserLocation hook silent first-mount prompt + 30-min TTL cache + DistancePill + LocationActions + /geolocation-test smoke page; Arc 2 (one session) wire 5 consumers + R3 analytics events. Net change toward investor narrative: digital-to-physical bridge thesis sharpens further — R17 is the **first feature where the digital surface MEASURES the physical bridge, not just describes it**. Distance + native-maps deep-link is the most concrete possible "go visit this real place" affordance. Roadmap: 17 R-rows total (was 16). 10 ✅ Shipped, 2 🟢 Ready (R3, R17), 5 🟡 Captured. Compounding disciplines validated this session: (a) reference-first → V1 (structural axes) → V2 (fill-refinement axes only) chain is now a 5-firing repeatable pattern that converts unclear-shape feature asks into design-to-Ready in single session; (b) `feedback_v2_mockup_as_fill_refinement` 3rd firing — promotion-ready as Tech Rule; (c) design-record-as-commitment-ceremony per session-56 rule continues to be the right shape for design-pass sessions; (d) `feedback_visual_reference_enumerate_candidates` honored — David's reference mockup ("reference only") treated as capability-level guidance, not pixel-level spec, leaving room for V1 → V2 iteration to find the right shape rather than replicating the reference 1:1. **Next natural investor-update trigger point**: now after R17 ships end-to-end (likely sessions 118–119 — Arc 1 + Arc 2). The investor narrative for the next update has a clean shape: three R-rows graduate same-day (R16 / R17) or end-to-end across 4 sessions (R10 / R1) using a memory-backed reference-first → mockup-chain → design-record-commit pattern that's now repeatable, and the digital-to-physical bridge thesis crosses from describe-the-place to measure-the-place territory.** **Session 118 was R17 Arc 1 — geolocation primitives shipped in isolation — 6 runtime commits sequenced smallest→largest with build clean at every commit boundary, David's iPhone QA on /geolocation-test passed clean. Pure execution pass against the 21-decision spec frozen at session 117; zero design calls, zero reversals, zero re-scoping. NEW pattern surfaced + worth tracking: *"Design-record-as-execution-spec — implementation runs against frozen decisions without re-scoping."* Single firing this session; Tech Rule candidate on second firing. Cleanest realization of the session-56 design-record-as-commitment-ceremony rule since R16 Task 4 (session 102). Implication: a fully-scoped design record reduces implementation cost by eliminating decision-overhead. Net runtime change: +600 lines across 6 new files (lib/distance.ts haversine + lib/mapsDeepLink.ts UA-aware URL builder + lib/useUserLocation.ts hook with TTL cache + cross-instance sync + <DistancePill> primitive + <LocationActions> primitive + /geolocation-test smoke page). All new code is opt-in — production consumers wire in Arc 2. Memory firings: smallest→largest commit sequencing >99 cumulative firings (was 93); testbed-first-for-AI-unknowns 3-firings deep across primitive ships (postcard-test + search-bar-test + geolocation-test); browser-API-in-useState-initializer-hydration honored proactively (hook defaults to INITIAL idle state, cache read in useEffect). Stopping point per planned cadence: David offered (a) close per session-117 opener vs (b) push into Arc 2 same session; picked close (Arc 2 deserves dedicated iPhone QA against real production seeded data). **Net change toward investor narrative**: sessions 117 + 118 together demonstrate "spec it once thoroughly, ship it cleanly without re-scoping" as a repeatable 2-session arc that converts unclear-shape feature asks into shipping primitives in ~2 sessions. The compounding investor shape continues: each R-item closure stacks onto a project posture where the design philosophy is articulated, the operational disciplines are memory-backed, and the codebase grows in capability with disciplined per-session scope. Roadmap unchanged at row level: 17 R-rows, 10 ✅ Shipped, 2 🟢 Ready (R3, R17 — Arc 1 of 2), 5 🟡 Captured. R17 graduates ✅ Shipped only after Arc 2 wires consumers + R3 events fire on live surfaces — likely session 119, after which the next natural investor-update trigger fires.**
