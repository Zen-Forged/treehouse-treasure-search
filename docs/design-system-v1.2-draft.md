# v1.2 DRAFT — RETIRED
> This draft was superseded during session 28. The approach changed mid-session from "migrate /post onto v1.1l chrome" to "retire /post in favor of a bottom sheet from /my-shelf." The canonical v1.2 spec now lives in `docs/design-system-v1.2-build-spec.md`, and the source-of-truth decisions are in the three mockups under `docs/mockups/` (add-find-sheet, review-page, edit-listing).

Do not read this file for v1.2 guidance. It remains on disk only until the code sprint completes, at which point it can be deleted.
